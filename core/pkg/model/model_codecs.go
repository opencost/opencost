////////////////////////////////////////////////////////////////////////////////
//
//                             DO NOT MODIFY
//
//                          ┻━┻ ︵ヽ(`Д´)ﾉ︵ ┻━┻
//
//
//            This source file was automatically generated by bingen.
//
////////////////////////////////////////////////////////////////////////////////

package model

import (
	"fmt"
	util "github.com/opencost/opencost/core/pkg/util"
	"reflect"
	"strings"
	"sync"
	"time"
)

const (
	// GeneratorPackageName is the package the generator is targetting
	GeneratorPackageName string = "model"
)

// BinaryTags represent the formatting tag used for specific optimization features
const (
	// BinaryTagStringTable is written and/or read prior to the existence of a string
	// table (where each index is encoded as a string entry in the resource
	BinaryTagStringTable string = "BGST"
)

const (
	// KubemodelCodecVersion is used for any resources listed in the Kubemodel version set
	KubemodelCodecVersion uint8 = 1
)

//--------------------------------------------------------------------------
//  Type Map
//--------------------------------------------------------------------------

// Generated type map for resolving interface implementations to
// to concrete types
var typeMap map[string]reflect.Type = map[string]reflect.Type{
	"Cluster":               reflect.TypeOf((*Cluster)(nil)).Elem(),
	"Container":             reflect.TypeOf((*Container)(nil)).Elem(),
	"Controller":            reflect.TypeOf((*Controller)(nil)).Elem(),
	"DiagnosticResult":      reflect.TypeOf((*DiagnosticResult)(nil)).Elem(),
	"GPUDevice":             reflect.TypeOf((*GPUDevice)(nil)).Elem(),
	"GPUUsage":              reflect.TypeOf((*GPUUsage)(nil)).Elem(),
	"Namespace":             reflect.TypeOf((*Namespace)(nil)).Elem(),
	"Node":                  reflect.TypeOf((*Node)(nil)).Elem(),
	"PersistentVolumeClaim": reflect.TypeOf((*PersistentVolumeClaim)(nil)).Elem(),
	"Pod":                   reflect.TypeOf((*Pod)(nil)).Elem(),
	"Service":               reflect.TypeOf((*Service)(nil)).Elem(),
	"ServicePort":           reflect.TypeOf((*ServicePort)(nil)).Elem(),
	"Volume":                reflect.TypeOf((*Volume)(nil)).Elem(),
	"Window":                reflect.TypeOf((*Window)(nil)).Elem(),
}

//--------------------------------------------------------------------------
//  Type Helpers
//--------------------------------------------------------------------------

// isBinaryTag returns true when the first bytes in the provided binary matches the tag
func isBinaryTag(data []byte, tag string) bool {
	return string(data[:len(tag)]) == tag
}

// appendBytes combines a and b into a new byte array
func appendBytes(a []byte, b []byte) []byte {
	al := len(a)
	bl := len(b)
	tl := al + bl

	// allocate a new byte array for the combined
	// use native copy for speedy byte copying
	result := make([]byte, tl, tl)
	copy(result, a)
	copy(result[al:], b)

	return result
}

// typeToString determines the basic properties of the type, the qualifier, package path, and
// type name, and returns the qualified type
func typeToString(f interface{}) string {
	qual := ""
	t := reflect.TypeOf(f)
	if t.Kind() == reflect.Ptr {
		t = t.Elem()
		qual = "*"
	}

	return fmt.Sprintf("%s%s.%s", qual, t.PkgPath(), t.Name())
}

// resolveType uses the name of a type and returns the package, base type name, and whether
// or not it's a pointer.
func resolveType(t string) (pkg string, name string, isPtr bool) {
	isPtr = t[:1] == "*"
	if isPtr {
		t = t[1:]
	}

	slashIndex := strings.LastIndex(t, "/")
	if slashIndex >= 0 {
		t = t[slashIndex+1:]
	}
	parts := strings.Split(t, ".")
	if parts[0] == GeneratorPackageName {
		parts[0] = ""
	}

	pkg = parts[0]
	name = parts[1]
	return
}

//--------------------------------------------------------------------------
//  StringTable
//--------------------------------------------------------------------------

// StringTable maps strings to specific indices for encoding
type StringTable struct {
	l       *sync.Mutex
	indices map[string]int
	next    int
}

// NewStringTable Creates a new StringTable instance with provided contents
func NewStringTable(contents ...string) *StringTable {
	st := &StringTable{
		l:       new(sync.Mutex),
		indices: make(map[string]int),
		next:    len(contents),
	}

	for i, entry := range contents {
		st.indices[entry] = i
	}

	return st
}

// AddOrGet atomically retrieves a string entry's index if it exist. Otherwise, it will
// add the entry and return the index.
func (st *StringTable) AddOrGet(s string) int {
	st.l.Lock()
	defer st.l.Unlock()

	if ind, ok := st.indices[s]; ok {
		return ind
	}

	current := st.next
	st.next++

	st.indices[s] = current
	return current
}

// ToSlice Converts the contents to a string array for encoding.
func (st *StringTable) ToSlice() []string {
	st.l.Lock()
	defer st.l.Unlock()

	if st.next == 0 {
		return []string{}
	}

	sl := make([]string, st.next, st.next)
	for s, i := range st.indices {
		sl[i] = s
	}
	return sl
}

// ToBytes Converts the contents to a binary encoded representation
func (st *StringTable) ToBytes() []byte {
	buff := util.NewBuffer()
	buff.WriteBytes([]byte(BinaryTagStringTable)) // bingen table header

	strs := st.ToSlice()

	buff.WriteInt(len(strs)) // table length
	for _, s := range strs {
		buff.WriteString(s)
	}

	return buff.Bytes()
}

//--------------------------------------------------------------------------
//  Codec Context
//--------------------------------------------------------------------------

// EncodingContext is a context object passed to the encoders to ensure reuse of buffer
// and table data
type EncodingContext struct {
	Buffer *util.Buffer
	Table  *StringTable
}

// IsStringTable returns true if the table is available
func (ec *EncodingContext) IsStringTable() bool {
	return ec.Table != nil
}

// DecodingContext is a context object passed to the decoders to ensure parent objects
// reuse as much data as possible
type DecodingContext struct {
	Buffer *util.Buffer
	Table  []string
}

// IsStringTable returns true if the table is available
func (dc *DecodingContext) IsStringTable() bool {
	return len(dc.Table) > 0
}

//--------------------------------------------------------------------------
//  Binary Codec
//--------------------------------------------------------------------------

// BinEncoder is an encoding interface which defines a context based marshal contract.
type BinEncoder interface {
	MarshalBinaryWithContext(*EncodingContext) error
}

// BinDecoder is a decoding interface which defines a context based unmarshal contract.
type BinDecoder interface {
	UnmarshalBinaryWithContext(*DecodingContext) error
}

//--------------------------------------------------------------------------
//  Cluster
//--------------------------------------------------------------------------

// MarshalBinary serializes the internal properties of this Cluster instance
// into a byte array
func (target *Cluster) MarshalBinary() (data []byte, err error) {
	ctx := &EncodingContext{
		Buffer: util.NewBuffer(),
		Table:  nil,
	}

	e := target.MarshalBinaryWithContext(ctx)
	if e != nil {
		return nil, e
	}

	encBytes := ctx.Buffer.Bytes()
	return encBytes, nil
}

// MarshalBinaryWithContext serializes the internal properties of this Cluster instance
// into a byte array leveraging a predefined context.
func (target *Cluster) MarshalBinaryWithContext(ctx *EncodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	buff.WriteUInt8(KubemodelCodecVersion) // version

	if ctx.IsStringTable() {
		a := ctx.Table.AddOrGet(target.ID)
		buff.WriteInt(a) // write table index
	} else {
		buff.WriteString(target.ID) // write string
	}
	// --- [begin][write][alias](Provider) ---
	if ctx.IsStringTable() {
		b := ctx.Table.AddOrGet(string(target.Provider))
		buff.WriteInt(b) // write table index
	} else {
		buff.WriteString(string(target.Provider)) // write string
	}
	// --- [end][write][alias](Provider) ---

	if ctx.IsStringTable() {
		c := ctx.Table.AddOrGet(target.Account)
		buff.WriteInt(c) // write table index
	} else {
		buff.WriteString(target.Account) // write string
	}
	if ctx.IsStringTable() {
		d := ctx.Table.AddOrGet(target.Name)
		buff.WriteInt(d) // write table index
	} else {
		buff.WriteString(target.Name) // write string
	}
	if target.Window == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][struct](Window) ---
		buff.WriteInt(0) // [compatibility, unused]
		errA := target.Window.MarshalBinaryWithContext(ctx)
		if errA != nil {
			return errA
		}
		// --- [end][write][struct](Window) ---

	}
	if target.Containers == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][slice]([]Container) ---
		buff.WriteInt(len(target.Containers)) // array length
		for i := 0; i < len(target.Containers); i++ {
			// --- [begin][write][struct](Container) ---
			buff.WriteInt(0) // [compatibility, unused]
			errB := target.Containers[i].MarshalBinaryWithContext(ctx)
			if errB != nil {
				return errB
			}
			// --- [end][write][struct](Container) ---

		}
		// --- [end][write][slice]([]Container) ---

	}
	if target.Controllers == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][slice]([]Controller) ---
		buff.WriteInt(len(target.Controllers)) // array length
		for j := 0; j < len(target.Controllers); j++ {
			// --- [begin][write][struct](Controller) ---
			buff.WriteInt(0) // [compatibility, unused]
			errC := target.Controllers[j].MarshalBinaryWithContext(ctx)
			if errC != nil {
				return errC
			}
			// --- [end][write][struct](Controller) ---

		}
		// --- [end][write][slice]([]Controller) ---

	}
	if target.Diagnostics == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][slice]([]DiagnosticResult) ---
		buff.WriteInt(len(target.Diagnostics)) // array length
		for ii := 0; ii < len(target.Diagnostics); ii++ {
			// --- [begin][write][struct](DiagnosticResult) ---
			buff.WriteInt(0) // [compatibility, unused]
			errD := target.Diagnostics[ii].MarshalBinaryWithContext(ctx)
			if errD != nil {
				return errD
			}
			// --- [end][write][struct](DiagnosticResult) ---

		}
		// --- [end][write][slice]([]DiagnosticResult) ---

	}
	if target.GpuDevices == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][slice]([]GPUDevice) ---
		buff.WriteInt(len(target.GpuDevices)) // array length
		for jj := 0; jj < len(target.GpuDevices); jj++ {
			// --- [begin][write][struct](GPUDevice) ---
			buff.WriteInt(0) // [compatibility, unused]
			errE := target.GpuDevices[jj].MarshalBinaryWithContext(ctx)
			if errE != nil {
				return errE
			}
			// --- [end][write][struct](GPUDevice) ---

		}
		// --- [end][write][slice]([]GPUDevice) ---

	}
	if target.GpuUsage == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][slice]([]GPUUsage) ---
		buff.WriteInt(len(target.GpuUsage)) // array length
		for iii := 0; iii < len(target.GpuUsage); iii++ {
			// --- [begin][write][struct](GPUUsage) ---
			buff.WriteInt(0) // [compatibility, unused]
			errF := target.GpuUsage[iii].MarshalBinaryWithContext(ctx)
			if errF != nil {
				return errF
			}
			// --- [end][write][struct](GPUUsage) ---

		}
		// --- [end][write][slice]([]GPUUsage) ---

	}
	if target.Namespaces == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][slice]([]Namespace) ---
		buff.WriteInt(len(target.Namespaces)) // array length
		for jjj := 0; jjj < len(target.Namespaces); jjj++ {
			// --- [begin][write][struct](Namespace) ---
			buff.WriteInt(0) // [compatibility, unused]
			errG := target.Namespaces[jjj].MarshalBinaryWithContext(ctx)
			if errG != nil {
				return errG
			}
			// --- [end][write][struct](Namespace) ---

		}
		// --- [end][write][slice]([]Namespace) ---

	}
	if target.Services == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][slice]([]Service) ---
		buff.WriteInt(len(target.Services)) // array length
		for iiii := 0; iiii < len(target.Services); iiii++ {
			// --- [begin][write][struct](Service) ---
			buff.WriteInt(0) // [compatibility, unused]
			errH := target.Services[iiii].MarshalBinaryWithContext(ctx)
			if errH != nil {
				return errH
			}
			// --- [end][write][struct](Service) ---

		}
		// --- [end][write][slice]([]Service) ---

	}
	if target.Nodes == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][slice]([]Node) ---
		buff.WriteInt(len(target.Nodes)) // array length
		for jjjj := 0; jjjj < len(target.Nodes); jjjj++ {
			// --- [begin][write][struct](Node) ---
			buff.WriteInt(0) // [compatibility, unused]
			errI := target.Nodes[jjjj].MarshalBinaryWithContext(ctx)
			if errI != nil {
				return errI
			}
			// --- [end][write][struct](Node) ---

		}
		// --- [end][write][slice]([]Node) ---

	}
	if target.Pods == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][slice]([]Pod) ---
		buff.WriteInt(len(target.Pods)) // array length
		for iiiii := 0; iiiii < len(target.Pods); iiiii++ {
			// --- [begin][write][struct](Pod) ---
			buff.WriteInt(0) // [compatibility, unused]
			errJ := target.Pods[iiiii].MarshalBinaryWithContext(ctx)
			if errJ != nil {
				return errJ
			}
			// --- [end][write][struct](Pod) ---

		}
		// --- [end][write][slice]([]Pod) ---

	}
	if target.Volumes == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][slice]([]Volume) ---
		buff.WriteInt(len(target.Volumes)) // array length
		for jjjjj := 0; jjjjj < len(target.Volumes); jjjjj++ {
			// --- [begin][write][struct](Volume) ---
			buff.WriteInt(0) // [compatibility, unused]
			errK := target.Volumes[jjjjj].MarshalBinaryWithContext(ctx)
			if errK != nil {
				return errK
			}
			// --- [end][write][struct](Volume) ---

		}
		// --- [end][write][slice]([]Volume) ---

	}
	if target.PersistentVolumeClaims == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][slice]([]PersistentVolumeClaim) ---
		buff.WriteInt(len(target.PersistentVolumeClaims)) // array length
		for iiiiii := 0; iiiiii < len(target.PersistentVolumeClaims); iiiiii++ {
			// --- [begin][write][struct](PersistentVolumeClaim) ---
			buff.WriteInt(0) // [compatibility, unused]
			errL := target.PersistentVolumeClaims[iiiiii].MarshalBinaryWithContext(ctx)
			if errL != nil {
				return errL
			}
			// --- [end][write][struct](PersistentVolumeClaim) ---

		}
		// --- [end][write][slice]([]PersistentVolumeClaim) ---

	}
	return nil
}

// UnmarshalBinary uses the data passed byte array to set all the internal properties of
// the Cluster type
func (target *Cluster) UnmarshalBinary(data []byte) error {
	var table []string
	buff := util.NewBufferFromBytes(data)

	// string table header validation
	if isBinaryTag(data, BinaryTagStringTable) {
		buff.ReadBytes(len(BinaryTagStringTable)) // strip tag length
		tl := buff.ReadInt()                      // table length
		if tl > 0 {
			table = make([]string, tl, tl)
			for i := 0; i < tl; i++ {
				table[i] = buff.ReadString()
			}
		}
	}

	ctx := &DecodingContext{
		Buffer: buff,
		Table:  table,
	}

	err := target.UnmarshalBinaryWithContext(ctx)
	if err != nil {
		return err
	}

	return nil
}

// UnmarshalBinaryWithContext uses the context containing a string table and binary buffer to set all the internal properties of
// the Cluster type
func (target *Cluster) UnmarshalBinaryWithContext(ctx *DecodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	version := buff.ReadUInt8()

	if version > KubemodelCodecVersion {
		return fmt.Errorf("Invalid Version Unmarshaling Cluster. Expected %d or less, got %d", KubemodelCodecVersion, version)
	}

	var b string
	if ctx.IsStringTable() {
		c := buff.ReadInt() // read string index
		b = ctx.Table[c]
	} else {
		b = buff.ReadString() // read string
	}
	a := b
	target.ID = a

	// --- [begin][read][alias](Provider) ---
	var d string
	var f string
	if ctx.IsStringTable() {
		g := buff.ReadInt() // read string index
		f = ctx.Table[g]
	} else {
		f = buff.ReadString() // read string
	}
	e := f
	d = e

	target.Provider = Provider(d)
	// --- [end][read][alias](Provider) ---

	var k string
	if ctx.IsStringTable() {
		l := buff.ReadInt() // read string index
		k = ctx.Table[l]
	} else {
		k = buff.ReadString() // read string
	}
	h := k
	target.Account = h

	var n string
	if ctx.IsStringTable() {
		o := buff.ReadInt() // read string index
		n = ctx.Table[o]
	} else {
		n = buff.ReadString() // read string
	}
	m := n
	target.Name = m

	if buff.ReadUInt8() == uint8(0) {
		target.Window = nil
	} else {
		// --- [begin][read][struct](Window) ---
		p := &Window{}
		buff.ReadInt() // [compatibility, unused]
		errA := p.UnmarshalBinaryWithContext(ctx)
		if errA != nil {
			return errA
		}
		target.Window = p
		// --- [end][read][struct](Window) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.Containers = nil
	} else {
		// --- [begin][read][slice]([]Container) ---
		r := buff.ReadInt() // array len
		q := make([]Container, r)
		for i := 0; i < r; i++ {
			// --- [begin][read][struct](Container) ---
			t := &Container{}
			buff.ReadInt() // [compatibility, unused]
			errB := t.UnmarshalBinaryWithContext(ctx)
			if errB != nil {
				return errB
			}
			s := *t
			// --- [end][read][struct](Container) ---

			q[i] = s
		}
		target.Containers = q
		// --- [end][read][slice]([]Container) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.Controllers = nil
	} else {
		// --- [begin][read][slice]([]Controller) ---
		w := buff.ReadInt() // array len
		u := make([]Controller, w)
		for j := 0; j < w; j++ {
			// --- [begin][read][struct](Controller) ---
			y := &Controller{}
			buff.ReadInt() // [compatibility, unused]
			errC := y.UnmarshalBinaryWithContext(ctx)
			if errC != nil {
				return errC
			}
			x := *y
			// --- [end][read][struct](Controller) ---

			u[j] = x
		}
		target.Controllers = u
		// --- [end][read][slice]([]Controller) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.Diagnostics = nil
	} else {
		// --- [begin][read][slice]([]DiagnosticResult) ---
		bb := buff.ReadInt() // array len
		aa := make([]DiagnosticResult, bb)
		for ii := 0; ii < bb; ii++ {
			// --- [begin][read][struct](DiagnosticResult) ---
			dd := &DiagnosticResult{}
			buff.ReadInt() // [compatibility, unused]
			errD := dd.UnmarshalBinaryWithContext(ctx)
			if errD != nil {
				return errD
			}
			cc := *dd
			// --- [end][read][struct](DiagnosticResult) ---

			aa[ii] = cc
		}
		target.Diagnostics = aa
		// --- [end][read][slice]([]DiagnosticResult) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.GpuDevices = nil
	} else {
		// --- [begin][read][slice]([]GPUDevice) ---
		ff := buff.ReadInt() // array len
		ee := make([]GPUDevice, ff)
		for jj := 0; jj < ff; jj++ {
			// --- [begin][read][struct](GPUDevice) ---
			hh := &GPUDevice{}
			buff.ReadInt() // [compatibility, unused]
			errE := hh.UnmarshalBinaryWithContext(ctx)
			if errE != nil {
				return errE
			}
			gg := *hh
			// --- [end][read][struct](GPUDevice) ---

			ee[jj] = gg
		}
		target.GpuDevices = ee
		// --- [end][read][slice]([]GPUDevice) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.GpuUsage = nil
	} else {
		// --- [begin][read][slice]([]GPUUsage) ---
		ll := buff.ReadInt() // array len
		kk := make([]GPUUsage, ll)
		for iii := 0; iii < ll; iii++ {
			// --- [begin][read][struct](GPUUsage) ---
			nn := &GPUUsage{}
			buff.ReadInt() // [compatibility, unused]
			errF := nn.UnmarshalBinaryWithContext(ctx)
			if errF != nil {
				return errF
			}
			mm := *nn
			// --- [end][read][struct](GPUUsage) ---

			kk[iii] = mm
		}
		target.GpuUsage = kk
		// --- [end][read][slice]([]GPUUsage) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.Namespaces = nil
	} else {
		// --- [begin][read][slice]([]Namespace) ---
		pp := buff.ReadInt() // array len
		oo := make([]Namespace, pp)
		for jjj := 0; jjj < pp; jjj++ {
			// --- [begin][read][struct](Namespace) ---
			rr := &Namespace{}
			buff.ReadInt() // [compatibility, unused]
			errG := rr.UnmarshalBinaryWithContext(ctx)
			if errG != nil {
				return errG
			}
			qq := *rr
			// --- [end][read][struct](Namespace) ---

			oo[jjj] = qq
		}
		target.Namespaces = oo
		// --- [end][read][slice]([]Namespace) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.Services = nil
	} else {
		// --- [begin][read][slice]([]Service) ---
		tt := buff.ReadInt() // array len
		ss := make([]Service, tt)
		for iiii := 0; iiii < tt; iiii++ {
			// --- [begin][read][struct](Service) ---
			ww := &Service{}
			buff.ReadInt() // [compatibility, unused]
			errH := ww.UnmarshalBinaryWithContext(ctx)
			if errH != nil {
				return errH
			}
			uu := *ww
			// --- [end][read][struct](Service) ---

			ss[iiii] = uu
		}
		target.Services = ss
		// --- [end][read][slice]([]Service) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.Nodes = nil
	} else {
		// --- [begin][read][slice]([]Node) ---
		yy := buff.ReadInt() // array len
		xx := make([]Node, yy)
		for jjjj := 0; jjjj < yy; jjjj++ {
			// --- [begin][read][struct](Node) ---
			bbb := &Node{}
			buff.ReadInt() // [compatibility, unused]
			errI := bbb.UnmarshalBinaryWithContext(ctx)
			if errI != nil {
				return errI
			}
			aaa := *bbb
			// --- [end][read][struct](Node) ---

			xx[jjjj] = aaa
		}
		target.Nodes = xx
		// --- [end][read][slice]([]Node) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.Pods = nil
	} else {
		// --- [begin][read][slice]([]Pod) ---
		ddd := buff.ReadInt() // array len
		ccc := make([]Pod, ddd)
		for iiiii := 0; iiiii < ddd; iiiii++ {
			// --- [begin][read][struct](Pod) ---
			fff := &Pod{}
			buff.ReadInt() // [compatibility, unused]
			errJ := fff.UnmarshalBinaryWithContext(ctx)
			if errJ != nil {
				return errJ
			}
			eee := *fff
			// --- [end][read][struct](Pod) ---

			ccc[iiiii] = eee
		}
		target.Pods = ccc
		// --- [end][read][slice]([]Pod) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.Volumes = nil
	} else {
		// --- [begin][read][slice]([]Volume) ---
		hhh := buff.ReadInt() // array len
		ggg := make([]Volume, hhh)
		for jjjjj := 0; jjjjj < hhh; jjjjj++ {
			// --- [begin][read][struct](Volume) ---
			lll := &Volume{}
			buff.ReadInt() // [compatibility, unused]
			errK := lll.UnmarshalBinaryWithContext(ctx)
			if errK != nil {
				return errK
			}
			kkk := *lll
			// --- [end][read][struct](Volume) ---

			ggg[jjjjj] = kkk
		}
		target.Volumes = ggg
		// --- [end][read][slice]([]Volume) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.PersistentVolumeClaims = nil
	} else {
		// --- [begin][read][slice]([]PersistentVolumeClaim) ---
		nnn := buff.ReadInt() // array len
		mmm := make([]PersistentVolumeClaim, nnn)
		for iiiiii := 0; iiiiii < nnn; iiiiii++ {
			// --- [begin][read][struct](PersistentVolumeClaim) ---
			ppp := &PersistentVolumeClaim{}
			buff.ReadInt() // [compatibility, unused]
			errL := ppp.UnmarshalBinaryWithContext(ctx)
			if errL != nil {
				return errL
			}
			ooo := *ppp
			// --- [end][read][struct](PersistentVolumeClaim) ---

			mmm[iiiiii] = ooo
		}
		target.PersistentVolumeClaims = mmm
		// --- [end][read][slice]([]PersistentVolumeClaim) ---

	}
	return nil
}

//--------------------------------------------------------------------------
//  Container
//--------------------------------------------------------------------------

// MarshalBinary serializes the internal properties of this Container instance
// into a byte array
func (target *Container) MarshalBinary() (data []byte, err error) {
	ctx := &EncodingContext{
		Buffer: util.NewBuffer(),
		Table:  nil,
	}

	e := target.MarshalBinaryWithContext(ctx)
	if e != nil {
		return nil, e
	}

	encBytes := ctx.Buffer.Bytes()
	return encBytes, nil
}

// MarshalBinaryWithContext serializes the internal properties of this Container instance
// into a byte array leveraging a predefined context.
func (target *Container) MarshalBinaryWithContext(ctx *EncodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	buff.WriteUInt8(KubemodelCodecVersion) // version

	if ctx.IsStringTable() {
		a := ctx.Table.AddOrGet(target.PodID)
		buff.WriteInt(a) // write table index
	} else {
		buff.WriteString(target.PodID) // write string
	}
	if ctx.IsStringTable() {
		b := ctx.Table.AddOrGet(target.Name)
		buff.WriteInt(b) // write table index
	} else {
		buff.WriteString(target.Name) // write string
	}
	if target.CreationTime == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][reference](time.Time) ---
		c, errA := target.CreationTime.MarshalBinary()
		if errA != nil {
			return errA
		}
		buff.WriteInt(len(c))
		buff.WriteBytes(c)
		// --- [end][write][reference](time.Time) ---

	}
	if target.DeletionTime == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][reference](time.Time) ---
		d, errB := target.DeletionTime.MarshalBinary()
		if errB != nil {
			return errB
		}
		buff.WriteInt(len(d))
		buff.WriteBytes(d)
		// --- [end][write][reference](time.Time) ---

	}
	buff.WriteFloat32(target.CpuCoreHours)          // write float32
	buff.WriteFloat32(target.CpuCoreRequestAverage) // write float32
	buff.WriteFloat32(target.CpuCoreUsageAverage)   // write float32
	buff.WriteFloat32(target.CpuCoreUsageMax)       // write float32
	buff.WriteInt64(target.RamByteHours)            // write int64
	buff.WriteInt64(target.RamBytesRequestAverage)  // write int64
	buff.WriteInt64(target.RamBytesUsageAverage)    // write int64
	buff.WriteInt64(target.RamBytesUsageMax)        // write int64
	return nil
}

// UnmarshalBinary uses the data passed byte array to set all the internal properties of
// the Container type
func (target *Container) UnmarshalBinary(data []byte) error {
	var table []string
	buff := util.NewBufferFromBytes(data)

	// string table header validation
	if isBinaryTag(data, BinaryTagStringTable) {
		buff.ReadBytes(len(BinaryTagStringTable)) // strip tag length
		tl := buff.ReadInt()                      // table length
		if tl > 0 {
			table = make([]string, tl, tl)
			for i := 0; i < tl; i++ {
				table[i] = buff.ReadString()
			}
		}
	}

	ctx := &DecodingContext{
		Buffer: buff,
		Table:  table,
	}

	err := target.UnmarshalBinaryWithContext(ctx)
	if err != nil {
		return err
	}

	return nil
}

// UnmarshalBinaryWithContext uses the context containing a string table and binary buffer to set all the internal properties of
// the Container type
func (target *Container) UnmarshalBinaryWithContext(ctx *DecodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	version := buff.ReadUInt8()

	if version > KubemodelCodecVersion {
		return fmt.Errorf("Invalid Version Unmarshaling Container. Expected %d or less, got %d", KubemodelCodecVersion, version)
	}

	var b string
	if ctx.IsStringTable() {
		c := buff.ReadInt() // read string index
		b = ctx.Table[c]
	} else {
		b = buff.ReadString() // read string
	}
	a := b
	target.PodID = a

	var e string
	if ctx.IsStringTable() {
		f := buff.ReadInt() // read string index
		e = ctx.Table[f]
	} else {
		e = buff.ReadString() // read string
	}
	d := e
	target.Name = d

	if buff.ReadUInt8() == uint8(0) {
		target.CreationTime = nil
	} else {
		// --- [begin][read][reference](time.Time) ---
		g := &time.Time{}
		h := buff.ReadInt()    // byte array length
		k := buff.ReadBytes(h) // byte array
		errA := g.UnmarshalBinary(k)
		if errA != nil {
			return errA
		}
		target.CreationTime = g
		// --- [end][read][reference](time.Time) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.DeletionTime = nil
	} else {
		// --- [begin][read][reference](time.Time) ---
		l := &time.Time{}
		m := buff.ReadInt()    // byte array length
		n := buff.ReadBytes(m) // byte array
		errB := l.UnmarshalBinary(n)
		if errB != nil {
			return errB
		}
		target.DeletionTime = l
		// --- [end][read][reference](time.Time) ---

	}
	o := buff.ReadFloat32() // read float32
	target.CpuCoreHours = o

	p := buff.ReadFloat32() // read float32
	target.CpuCoreRequestAverage = p

	q := buff.ReadFloat32() // read float32
	target.CpuCoreUsageAverage = q

	r := buff.ReadFloat32() // read float32
	target.CpuCoreUsageMax = r

	s := buff.ReadInt64() // read int64
	target.RamByteHours = s

	t := buff.ReadInt64() // read int64
	target.RamBytesRequestAverage = t

	u := buff.ReadInt64() // read int64
	target.RamBytesUsageAverage = u

	w := buff.ReadInt64() // read int64
	target.RamBytesUsageMax = w

	return nil
}

//--------------------------------------------------------------------------
//  Controller
//--------------------------------------------------------------------------

// MarshalBinary serializes the internal properties of this Controller instance
// into a byte array
func (target *Controller) MarshalBinary() (data []byte, err error) {
	ctx := &EncodingContext{
		Buffer: util.NewBuffer(),
		Table:  nil,
	}

	e := target.MarshalBinaryWithContext(ctx)
	if e != nil {
		return nil, e
	}

	encBytes := ctx.Buffer.Bytes()
	return encBytes, nil
}

// MarshalBinaryWithContext serializes the internal properties of this Controller instance
// into a byte array leveraging a predefined context.
func (target *Controller) MarshalBinaryWithContext(ctx *EncodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	buff.WriteUInt8(KubemodelCodecVersion) // version

	if ctx.IsStringTable() {
		a := ctx.Table.AddOrGet(target.ID)
		buff.WriteInt(a) // write table index
	} else {
		buff.WriteString(target.ID) // write string
	}
	if ctx.IsStringTable() {
		b := ctx.Table.AddOrGet(target.NamespaceID)
		buff.WriteInt(b) // write table index
	} else {
		buff.WriteString(target.NamespaceID) // write string
	}
	if ctx.IsStringTable() {
		c := ctx.Table.AddOrGet(target.Name)
		buff.WriteInt(c) // write table index
	} else {
		buff.WriteString(target.Name) // write string
	}
	// --- [begin][write][alias](ControllerKind) ---
	if ctx.IsStringTable() {
		d := ctx.Table.AddOrGet(string(target.Kind))
		buff.WriteInt(d) // write table index
	} else {
		buff.WriteString(string(target.Kind)) // write string
	}
	// --- [end][write][alias](ControllerKind) ---

	if target.Labels == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][map](map[string]string) ---
		buff.WriteInt(len(target.Labels)) // map length
		for v, z := range target.Labels {
			if ctx.IsStringTable() {
				e := ctx.Table.AddOrGet(v)
				buff.WriteInt(e) // write table index
			} else {
				buff.WriteString(v) // write string
			}
			if ctx.IsStringTable() {
				f := ctx.Table.AddOrGet(z)
				buff.WriteInt(f) // write table index
			} else {
				buff.WriteString(z) // write string
			}
		}
		// --- [end][write][map](map[string]string) ---

	}
	if target.Annotations == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][map](map[string]string) ---
		buff.WriteInt(len(target.Annotations)) // map length
		for vv, zz := range target.Annotations {
			if ctx.IsStringTable() {
				g := ctx.Table.AddOrGet(vv)
				buff.WriteInt(g) // write table index
			} else {
				buff.WriteString(vv) // write string
			}
			if ctx.IsStringTable() {
				h := ctx.Table.AddOrGet(zz)
				buff.WriteInt(h) // write table index
			} else {
				buff.WriteString(zz) // write string
			}
		}
		// --- [end][write][map](map[string]string) ---

	}
	if target.CreationTime == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][reference](time.Time) ---
		k, errA := target.CreationTime.MarshalBinary()
		if errA != nil {
			return errA
		}
		buff.WriteInt(len(k))
		buff.WriteBytes(k)
		// --- [end][write][reference](time.Time) ---

	}
	if target.DeletionTime == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][reference](time.Time) ---
		l, errB := target.DeletionTime.MarshalBinary()
		if errB != nil {
			return errB
		}
		buff.WriteInt(len(l))
		buff.WriteBytes(l)
		// --- [end][write][reference](time.Time) ---

	}
	if target.Diagnostic == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][struct](DiagnosticResult) ---
		buff.WriteInt(0) // [compatibility, unused]
		errC := target.Diagnostic.MarshalBinaryWithContext(ctx)
		if errC != nil {
			return errC
		}
		// --- [end][write][struct](DiagnosticResult) ---

	}
	return nil
}

// UnmarshalBinary uses the data passed byte array to set all the internal properties of
// the Controller type
func (target *Controller) UnmarshalBinary(data []byte) error {
	var table []string
	buff := util.NewBufferFromBytes(data)

	// string table header validation
	if isBinaryTag(data, BinaryTagStringTable) {
		buff.ReadBytes(len(BinaryTagStringTable)) // strip tag length
		tl := buff.ReadInt()                      // table length
		if tl > 0 {
			table = make([]string, tl, tl)
			for i := 0; i < tl; i++ {
				table[i] = buff.ReadString()
			}
		}
	}

	ctx := &DecodingContext{
		Buffer: buff,
		Table:  table,
	}

	err := target.UnmarshalBinaryWithContext(ctx)
	if err != nil {
		return err
	}

	return nil
}

// UnmarshalBinaryWithContext uses the context containing a string table and binary buffer to set all the internal properties of
// the Controller type
func (target *Controller) UnmarshalBinaryWithContext(ctx *DecodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	version := buff.ReadUInt8()

	if version > KubemodelCodecVersion {
		return fmt.Errorf("Invalid Version Unmarshaling Controller. Expected %d or less, got %d", KubemodelCodecVersion, version)
	}

	var b string
	if ctx.IsStringTable() {
		c := buff.ReadInt() // read string index
		b = ctx.Table[c]
	} else {
		b = buff.ReadString() // read string
	}
	a := b
	target.ID = a

	var e string
	if ctx.IsStringTable() {
		f := buff.ReadInt() // read string index
		e = ctx.Table[f]
	} else {
		e = buff.ReadString() // read string
	}
	d := e
	target.NamespaceID = d

	var h string
	if ctx.IsStringTable() {
		k := buff.ReadInt() // read string index
		h = ctx.Table[k]
	} else {
		h = buff.ReadString() // read string
	}
	g := h
	target.Name = g

	// --- [begin][read][alias](ControllerKind) ---
	var l string
	var n string
	if ctx.IsStringTable() {
		o := buff.ReadInt() // read string index
		n = ctx.Table[o]
	} else {
		n = buff.ReadString() // read string
	}
	m := n
	l = m

	target.Kind = ControllerKind(l)
	// --- [end][read][alias](ControllerKind) ---

	if buff.ReadUInt8() == uint8(0) {
		target.Labels = nil
	} else {
		// --- [begin][read][map](map[string]string) ---
		q := buff.ReadInt() // map len
		p := make(map[string]string, q)
		for i := 0; i < q; i++ {
			var v string
			var s string
			if ctx.IsStringTable() {
				t := buff.ReadInt() // read string index
				s = ctx.Table[t]
			} else {
				s = buff.ReadString() // read string
			}
			r := s
			v = r

			var z string
			var w string
			if ctx.IsStringTable() {
				x := buff.ReadInt() // read string index
				w = ctx.Table[x]
			} else {
				w = buff.ReadString() // read string
			}
			u := w
			z = u

			p[v] = z
		}
		target.Labels = p
		// --- [end][read][map](map[string]string) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.Annotations = nil
	} else {
		// --- [begin][read][map](map[string]string) ---
		aa := buff.ReadInt() // map len
		y := make(map[string]string, aa)
		for j := 0; j < aa; j++ {
			var vv string
			var cc string
			if ctx.IsStringTable() {
				dd := buff.ReadInt() // read string index
				cc = ctx.Table[dd]
			} else {
				cc = buff.ReadString() // read string
			}
			bb := cc
			vv = bb

			var zz string
			var ff string
			if ctx.IsStringTable() {
				gg := buff.ReadInt() // read string index
				ff = ctx.Table[gg]
			} else {
				ff = buff.ReadString() // read string
			}
			ee := ff
			zz = ee

			y[vv] = zz
		}
		target.Annotations = y
		// --- [end][read][map](map[string]string) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.CreationTime = nil
	} else {
		// --- [begin][read][reference](time.Time) ---
		hh := &time.Time{}
		kk := buff.ReadInt()     // byte array length
		ll := buff.ReadBytes(kk) // byte array
		errA := hh.UnmarshalBinary(ll)
		if errA != nil {
			return errA
		}
		target.CreationTime = hh
		// --- [end][read][reference](time.Time) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.DeletionTime = nil
	} else {
		// --- [begin][read][reference](time.Time) ---
		mm := &time.Time{}
		nn := buff.ReadInt()     // byte array length
		oo := buff.ReadBytes(nn) // byte array
		errB := mm.UnmarshalBinary(oo)
		if errB != nil {
			return errB
		}
		target.DeletionTime = mm
		// --- [end][read][reference](time.Time) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.Diagnostic = nil
	} else {
		// --- [begin][read][struct](DiagnosticResult) ---
		pp := &DiagnosticResult{}
		buff.ReadInt() // [compatibility, unused]
		errC := pp.UnmarshalBinaryWithContext(ctx)
		if errC != nil {
			return errC
		}
		target.Diagnostic = pp
		// --- [end][read][struct](DiagnosticResult) ---

	}
	return nil
}

//--------------------------------------------------------------------------
//  DiagnosticResult
//--------------------------------------------------------------------------

// MarshalBinary serializes the internal properties of this DiagnosticResult instance
// into a byte array
func (target *DiagnosticResult) MarshalBinary() (data []byte, err error) {
	ctx := &EncodingContext{
		Buffer: util.NewBuffer(),
		Table:  nil,
	}

	e := target.MarshalBinaryWithContext(ctx)
	if e != nil {
		return nil, e
	}

	encBytes := ctx.Buffer.Bytes()
	return encBytes, nil
}

// MarshalBinaryWithContext serializes the internal properties of this DiagnosticResult instance
// into a byte array leveraging a predefined context.
func (target *DiagnosticResult) MarshalBinaryWithContext(ctx *EncodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	buff.WriteUInt8(KubemodelCodecVersion) // version

	if ctx.IsStringTable() {
		a := ctx.Table.AddOrGet(target.Name)
		buff.WriteInt(a) // write table index
	} else {
		buff.WriteString(target.Name) // write string
	}
	if ctx.IsStringTable() {
		b := ctx.Table.AddOrGet(target.Status)
		buff.WriteInt(b) // write table index
	} else {
		buff.WriteString(target.Status) // write string
	}
	if ctx.IsStringTable() {
		c := ctx.Table.AddOrGet(target.Message)
		buff.WriteInt(c) // write table index
	} else {
		buff.WriteString(target.Message) // write string
	}
	if ctx.IsStringTable() {
		d := ctx.Table.AddOrGet(target.Severity)
		buff.WriteInt(d) // write table index
	} else {
		buff.WriteString(target.Severity) // write string
	}
	return nil
}

// UnmarshalBinary uses the data passed byte array to set all the internal properties of
// the DiagnosticResult type
func (target *DiagnosticResult) UnmarshalBinary(data []byte) error {
	var table []string
	buff := util.NewBufferFromBytes(data)

	// string table header validation
	if isBinaryTag(data, BinaryTagStringTable) {
		buff.ReadBytes(len(BinaryTagStringTable)) // strip tag length
		tl := buff.ReadInt()                      // table length
		if tl > 0 {
			table = make([]string, tl, tl)
			for i := 0; i < tl; i++ {
				table[i] = buff.ReadString()
			}
		}
	}

	ctx := &DecodingContext{
		Buffer: buff,
		Table:  table,
	}

	err := target.UnmarshalBinaryWithContext(ctx)
	if err != nil {
		return err
	}

	return nil
}

// UnmarshalBinaryWithContext uses the context containing a string table and binary buffer to set all the internal properties of
// the DiagnosticResult type
func (target *DiagnosticResult) UnmarshalBinaryWithContext(ctx *DecodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	version := buff.ReadUInt8()

	if version > KubemodelCodecVersion {
		return fmt.Errorf("Invalid Version Unmarshaling DiagnosticResult. Expected %d or less, got %d", KubemodelCodecVersion, version)
	}

	var b string
	if ctx.IsStringTable() {
		c := buff.ReadInt() // read string index
		b = ctx.Table[c]
	} else {
		b = buff.ReadString() // read string
	}
	a := b
	target.Name = a

	var e string
	if ctx.IsStringTable() {
		f := buff.ReadInt() // read string index
		e = ctx.Table[f]
	} else {
		e = buff.ReadString() // read string
	}
	d := e
	target.Status = d

	var h string
	if ctx.IsStringTable() {
		k := buff.ReadInt() // read string index
		h = ctx.Table[k]
	} else {
		h = buff.ReadString() // read string
	}
	g := h
	target.Message = g

	var m string
	if ctx.IsStringTable() {
		n := buff.ReadInt() // read string index
		m = ctx.Table[n]
	} else {
		m = buff.ReadString() // read string
	}
	l := m
	target.Severity = l

	return nil
}

//--------------------------------------------------------------------------
//  GPUDevice
//--------------------------------------------------------------------------

// MarshalBinary serializes the internal properties of this GPUDevice instance
// into a byte array
func (target *GPUDevice) MarshalBinary() (data []byte, err error) {
	ctx := &EncodingContext{
		Buffer: util.NewBuffer(),
		Table:  nil,
	}

	e := target.MarshalBinaryWithContext(ctx)
	if e != nil {
		return nil, e
	}

	encBytes := ctx.Buffer.Bytes()
	return encBytes, nil
}

// MarshalBinaryWithContext serializes the internal properties of this GPUDevice instance
// into a byte array leveraging a predefined context.
func (target *GPUDevice) MarshalBinaryWithContext(ctx *EncodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	buff.WriteUInt8(KubemodelCodecVersion) // version

	if ctx.IsStringTable() {
		a := ctx.Table.AddOrGet(target.ID)
		buff.WriteInt(a) // write table index
	} else {
		buff.WriteString(target.ID) // write string
	}
	if ctx.IsStringTable() {
		b := ctx.Table.AddOrGet(target.NodeID)
		buff.WriteInt(b) // write table index
	} else {
		buff.WriteString(target.NodeID) // write string
	}
	if ctx.IsStringTable() {
		c := ctx.Table.AddOrGet(target.Name)
		buff.WriteInt(c) // write table index
	} else {
		buff.WriteString(target.Name) // write string
	}
	if ctx.IsStringTable() {
		d := ctx.Table.AddOrGet(target.Model)
		buff.WriteInt(d) // write table index
	} else {
		buff.WriteString(target.Model) // write string
	}
	buff.WriteInt64(target.Memory) // write int64
	buff.WriteInt32(target.Count)  // write int32
	return nil
}

// UnmarshalBinary uses the data passed byte array to set all the internal properties of
// the GPUDevice type
func (target *GPUDevice) UnmarshalBinary(data []byte) error {
	var table []string
	buff := util.NewBufferFromBytes(data)

	// string table header validation
	if isBinaryTag(data, BinaryTagStringTable) {
		buff.ReadBytes(len(BinaryTagStringTable)) // strip tag length
		tl := buff.ReadInt()                      // table length
		if tl > 0 {
			table = make([]string, tl, tl)
			for i := 0; i < tl; i++ {
				table[i] = buff.ReadString()
			}
		}
	}

	ctx := &DecodingContext{
		Buffer: buff,
		Table:  table,
	}

	err := target.UnmarshalBinaryWithContext(ctx)
	if err != nil {
		return err
	}

	return nil
}

// UnmarshalBinaryWithContext uses the context containing a string table and binary buffer to set all the internal properties of
// the GPUDevice type
func (target *GPUDevice) UnmarshalBinaryWithContext(ctx *DecodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	version := buff.ReadUInt8()

	if version > KubemodelCodecVersion {
		return fmt.Errorf("Invalid Version Unmarshaling GPUDevice. Expected %d or less, got %d", KubemodelCodecVersion, version)
	}

	var b string
	if ctx.IsStringTable() {
		c := buff.ReadInt() // read string index
		b = ctx.Table[c]
	} else {
		b = buff.ReadString() // read string
	}
	a := b
	target.ID = a

	var e string
	if ctx.IsStringTable() {
		f := buff.ReadInt() // read string index
		e = ctx.Table[f]
	} else {
		e = buff.ReadString() // read string
	}
	d := e
	target.NodeID = d

	var h string
	if ctx.IsStringTable() {
		k := buff.ReadInt() // read string index
		h = ctx.Table[k]
	} else {
		h = buff.ReadString() // read string
	}
	g := h
	target.Name = g

	var m string
	if ctx.IsStringTable() {
		n := buff.ReadInt() // read string index
		m = ctx.Table[n]
	} else {
		m = buff.ReadString() // read string
	}
	l := m
	target.Model = l

	o := buff.ReadInt64() // read int64
	target.Memory = o

	p := buff.ReadInt32() // read int32
	target.Count = p

	return nil
}

//--------------------------------------------------------------------------
//  GPUUsage
//--------------------------------------------------------------------------

// MarshalBinary serializes the internal properties of this GPUUsage instance
// into a byte array
func (target *GPUUsage) MarshalBinary() (data []byte, err error) {
	ctx := &EncodingContext{
		Buffer: util.NewBuffer(),
		Table:  nil,
	}

	e := target.MarshalBinaryWithContext(ctx)
	if e != nil {
		return nil, e
	}

	encBytes := ctx.Buffer.Bytes()
	return encBytes, nil
}

// MarshalBinaryWithContext serializes the internal properties of this GPUUsage instance
// into a byte array leveraging a predefined context.
func (target *GPUUsage) MarshalBinaryWithContext(ctx *EncodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	buff.WriteUInt8(KubemodelCodecVersion) // version

	if ctx.IsStringTable() {
		a := ctx.Table.AddOrGet(target.ContainerID)
		buff.WriteInt(a) // write table index
	} else {
		buff.WriteString(target.ContainerID) // write string
	}
	if ctx.IsStringTable() {
		b := ctx.Table.AddOrGet(target.GpuDeviceID)
		buff.WriteInt(b) // write table index
	} else {
		buff.WriteString(target.GpuDeviceID) // write string
	}
	buff.WriteFloat32(target.UtilizationAverage) // write float32
	buff.WriteFloat32(target.UtilizationMax)     // write float32
	buff.WriteInt64(target.MemoryUsageAverage)   // write int64
	buff.WriteInt64(target.MemoryUsageMax)       // write int64
	return nil
}

// UnmarshalBinary uses the data passed byte array to set all the internal properties of
// the GPUUsage type
func (target *GPUUsage) UnmarshalBinary(data []byte) error {
	var table []string
	buff := util.NewBufferFromBytes(data)

	// string table header validation
	if isBinaryTag(data, BinaryTagStringTable) {
		buff.ReadBytes(len(BinaryTagStringTable)) // strip tag length
		tl := buff.ReadInt()                      // table length
		if tl > 0 {
			table = make([]string, tl, tl)
			for i := 0; i < tl; i++ {
				table[i] = buff.ReadString()
			}
		}
	}

	ctx := &DecodingContext{
		Buffer: buff,
		Table:  table,
	}

	err := target.UnmarshalBinaryWithContext(ctx)
	if err != nil {
		return err
	}

	return nil
}

// UnmarshalBinaryWithContext uses the context containing a string table and binary buffer to set all the internal properties of
// the GPUUsage type
func (target *GPUUsage) UnmarshalBinaryWithContext(ctx *DecodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	version := buff.ReadUInt8()

	if version > KubemodelCodecVersion {
		return fmt.Errorf("Invalid Version Unmarshaling GPUUsage. Expected %d or less, got %d", KubemodelCodecVersion, version)
	}

	var b string
	if ctx.IsStringTable() {
		c := buff.ReadInt() // read string index
		b = ctx.Table[c]
	} else {
		b = buff.ReadString() // read string
	}
	a := b
	target.ContainerID = a

	var e string
	if ctx.IsStringTable() {
		f := buff.ReadInt() // read string index
		e = ctx.Table[f]
	} else {
		e = buff.ReadString() // read string
	}
	d := e
	target.GpuDeviceID = d

	g := buff.ReadFloat32() // read float32
	target.UtilizationAverage = g

	h := buff.ReadFloat32() // read float32
	target.UtilizationMax = h

	k := buff.ReadInt64() // read int64
	target.MemoryUsageAverage = k

	l := buff.ReadInt64() // read int64
	target.MemoryUsageMax = l

	return nil
}

//--------------------------------------------------------------------------
//  Namespace
//--------------------------------------------------------------------------

// MarshalBinary serializes the internal properties of this Namespace instance
// into a byte array
func (target *Namespace) MarshalBinary() (data []byte, err error) {
	ctx := &EncodingContext{
		Buffer: util.NewBuffer(),
		Table:  nil,
	}

	e := target.MarshalBinaryWithContext(ctx)
	if e != nil {
		return nil, e
	}

	encBytes := ctx.Buffer.Bytes()
	return encBytes, nil
}

// MarshalBinaryWithContext serializes the internal properties of this Namespace instance
// into a byte array leveraging a predefined context.
func (target *Namespace) MarshalBinaryWithContext(ctx *EncodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	buff.WriteUInt8(KubemodelCodecVersion) // version

	if ctx.IsStringTable() {
		a := ctx.Table.AddOrGet(target.ID)
		buff.WriteInt(a) // write table index
	} else {
		buff.WriteString(target.ID) // write string
	}
	if ctx.IsStringTable() {
		b := ctx.Table.AddOrGet(target.ClusterID)
		buff.WriteInt(b) // write table index
	} else {
		buff.WriteString(target.ClusterID) // write string
	}
	if ctx.IsStringTable() {
		c := ctx.Table.AddOrGet(target.Name)
		buff.WriteInt(c) // write table index
	} else {
		buff.WriteString(target.Name) // write string
	}
	if target.Labels == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][map](map[string]string) ---
		buff.WriteInt(len(target.Labels)) // map length
		for v, z := range target.Labels {
			if ctx.IsStringTable() {
				d := ctx.Table.AddOrGet(v)
				buff.WriteInt(d) // write table index
			} else {
				buff.WriteString(v) // write string
			}
			if ctx.IsStringTable() {
				e := ctx.Table.AddOrGet(z)
				buff.WriteInt(e) // write table index
			} else {
				buff.WriteString(z) // write string
			}
		}
		// --- [end][write][map](map[string]string) ---

	}
	if target.Annotations == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][map](map[string]string) ---
		buff.WriteInt(len(target.Annotations)) // map length
		for vv, zz := range target.Annotations {
			if ctx.IsStringTable() {
				f := ctx.Table.AddOrGet(vv)
				buff.WriteInt(f) // write table index
			} else {
				buff.WriteString(vv) // write string
			}
			if ctx.IsStringTable() {
				g := ctx.Table.AddOrGet(zz)
				buff.WriteInt(g) // write table index
			} else {
				buff.WriteString(zz) // write string
			}
		}
		// --- [end][write][map](map[string]string) ---

	}
	if target.CreationTime == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][reference](time.Time) ---
		h, errA := target.CreationTime.MarshalBinary()
		if errA != nil {
			return errA
		}
		buff.WriteInt(len(h))
		buff.WriteBytes(h)
		// --- [end][write][reference](time.Time) ---

	}
	if target.DeletionTime == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][reference](time.Time) ---
		k, errB := target.DeletionTime.MarshalBinary()
		if errB != nil {
			return errB
		}
		buff.WriteInt(len(k))
		buff.WriteBytes(k)
		// --- [end][write][reference](time.Time) ---

	}
	if target.Diagnostic == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][struct](DiagnosticResult) ---
		buff.WriteInt(0) // [compatibility, unused]
		errC := target.Diagnostic.MarshalBinaryWithContext(ctx)
		if errC != nil {
			return errC
		}
		// --- [end][write][struct](DiagnosticResult) ---

	}
	return nil
}

// UnmarshalBinary uses the data passed byte array to set all the internal properties of
// the Namespace type
func (target *Namespace) UnmarshalBinary(data []byte) error {
	var table []string
	buff := util.NewBufferFromBytes(data)

	// string table header validation
	if isBinaryTag(data, BinaryTagStringTable) {
		buff.ReadBytes(len(BinaryTagStringTable)) // strip tag length
		tl := buff.ReadInt()                      // table length
		if tl > 0 {
			table = make([]string, tl, tl)
			for i := 0; i < tl; i++ {
				table[i] = buff.ReadString()
			}
		}
	}

	ctx := &DecodingContext{
		Buffer: buff,
		Table:  table,
	}

	err := target.UnmarshalBinaryWithContext(ctx)
	if err != nil {
		return err
	}

	return nil
}

// UnmarshalBinaryWithContext uses the context containing a string table and binary buffer to set all the internal properties of
// the Namespace type
func (target *Namespace) UnmarshalBinaryWithContext(ctx *DecodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	version := buff.ReadUInt8()

	if version > KubemodelCodecVersion {
		return fmt.Errorf("Invalid Version Unmarshaling Namespace. Expected %d or less, got %d", KubemodelCodecVersion, version)
	}

	var b string
	if ctx.IsStringTable() {
		c := buff.ReadInt() // read string index
		b = ctx.Table[c]
	} else {
		b = buff.ReadString() // read string
	}
	a := b
	target.ID = a

	var e string
	if ctx.IsStringTable() {
		f := buff.ReadInt() // read string index
		e = ctx.Table[f]
	} else {
		e = buff.ReadString() // read string
	}
	d := e
	target.ClusterID = d

	var h string
	if ctx.IsStringTable() {
		k := buff.ReadInt() // read string index
		h = ctx.Table[k]
	} else {
		h = buff.ReadString() // read string
	}
	g := h
	target.Name = g

	if buff.ReadUInt8() == uint8(0) {
		target.Labels = nil
	} else {
		// --- [begin][read][map](map[string]string) ---
		m := buff.ReadInt() // map len
		l := make(map[string]string, m)
		for i := 0; i < m; i++ {
			var v string
			var o string
			if ctx.IsStringTable() {
				p := buff.ReadInt() // read string index
				o = ctx.Table[p]
			} else {
				o = buff.ReadString() // read string
			}
			n := o
			v = n

			var z string
			var r string
			if ctx.IsStringTable() {
				s := buff.ReadInt() // read string index
				r = ctx.Table[s]
			} else {
				r = buff.ReadString() // read string
			}
			q := r
			z = q

			l[v] = z
		}
		target.Labels = l
		// --- [end][read][map](map[string]string) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.Annotations = nil
	} else {
		// --- [begin][read][map](map[string]string) ---
		u := buff.ReadInt() // map len
		t := make(map[string]string, u)
		for j := 0; j < u; j++ {
			var vv string
			var x string
			if ctx.IsStringTable() {
				y := buff.ReadInt() // read string index
				x = ctx.Table[y]
			} else {
				x = buff.ReadString() // read string
			}
			w := x
			vv = w

			var zz string
			var bb string
			if ctx.IsStringTable() {
				cc := buff.ReadInt() // read string index
				bb = ctx.Table[cc]
			} else {
				bb = buff.ReadString() // read string
			}
			aa := bb
			zz = aa

			t[vv] = zz
		}
		target.Annotations = t
		// --- [end][read][map](map[string]string) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.CreationTime = nil
	} else {
		// --- [begin][read][reference](time.Time) ---
		dd := &time.Time{}
		ee := buff.ReadInt()     // byte array length
		ff := buff.ReadBytes(ee) // byte array
		errA := dd.UnmarshalBinary(ff)
		if errA != nil {
			return errA
		}
		target.CreationTime = dd
		// --- [end][read][reference](time.Time) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.DeletionTime = nil
	} else {
		// --- [begin][read][reference](time.Time) ---
		gg := &time.Time{}
		hh := buff.ReadInt()     // byte array length
		kk := buff.ReadBytes(hh) // byte array
		errB := gg.UnmarshalBinary(kk)
		if errB != nil {
			return errB
		}
		target.DeletionTime = gg
		// --- [end][read][reference](time.Time) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.Diagnostic = nil
	} else {
		// --- [begin][read][struct](DiagnosticResult) ---
		ll := &DiagnosticResult{}
		buff.ReadInt() // [compatibility, unused]
		errC := ll.UnmarshalBinaryWithContext(ctx)
		if errC != nil {
			return errC
		}
		target.Diagnostic = ll
		// --- [end][read][struct](DiagnosticResult) ---

	}
	return nil
}

//--------------------------------------------------------------------------
//  Node
//--------------------------------------------------------------------------

// MarshalBinary serializes the internal properties of this Node instance
// into a byte array
func (target *Node) MarshalBinary() (data []byte, err error) {
	ctx := &EncodingContext{
		Buffer: util.NewBuffer(),
		Table:  nil,
	}

	e := target.MarshalBinaryWithContext(ctx)
	if e != nil {
		return nil, e
	}

	encBytes := ctx.Buffer.Bytes()
	return encBytes, nil
}

// MarshalBinaryWithContext serializes the internal properties of this Node instance
// into a byte array leveraging a predefined context.
func (target *Node) MarshalBinaryWithContext(ctx *EncodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	buff.WriteUInt8(KubemodelCodecVersion) // version

	if ctx.IsStringTable() {
		a := ctx.Table.AddOrGet(target.ID)
		buff.WriteInt(a) // write table index
	} else {
		buff.WriteString(target.ID) // write string
	}
	if ctx.IsStringTable() {
		b := ctx.Table.AddOrGet(target.ClusterID)
		buff.WriteInt(b) // write table index
	} else {
		buff.WriteString(target.ClusterID) // write string
	}
	if ctx.IsStringTable() {
		c := ctx.Table.AddOrGet(target.ProviderResourceID)
		buff.WriteInt(c) // write table index
	} else {
		buff.WriteString(target.ProviderResourceID) // write string
	}
	if ctx.IsStringTable() {
		d := ctx.Table.AddOrGet(target.Name)
		buff.WriteInt(d) // write table index
	} else {
		buff.WriteString(target.Name) // write string
	}
	if target.Labels == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][map](map[string]string) ---
		buff.WriteInt(len(target.Labels)) // map length
		for v, z := range target.Labels {
			if ctx.IsStringTable() {
				e := ctx.Table.AddOrGet(v)
				buff.WriteInt(e) // write table index
			} else {
				buff.WriteString(v) // write string
			}
			if ctx.IsStringTable() {
				f := ctx.Table.AddOrGet(z)
				buff.WriteInt(f) // write table index
			} else {
				buff.WriteString(z) // write string
			}
		}
		// --- [end][write][map](map[string]string) ---

	}
	if target.Annotations == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][map](map[string]string) ---
		buff.WriteInt(len(target.Annotations)) // map length
		for vv, zz := range target.Annotations {
			if ctx.IsStringTable() {
				g := ctx.Table.AddOrGet(vv)
				buff.WriteInt(g) // write table index
			} else {
				buff.WriteString(vv) // write string
			}
			if ctx.IsStringTable() {
				h := ctx.Table.AddOrGet(zz)
				buff.WriteInt(h) // write table index
			} else {
				buff.WriteString(zz) // write string
			}
		}
		// --- [end][write][map](map[string]string) ---

	}
	if target.CreationTime == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][reference](time.Time) ---
		k, errA := target.CreationTime.MarshalBinary()
		if errA != nil {
			return errA
		}
		buff.WriteInt(len(k))
		buff.WriteBytes(k)
		// --- [end][write][reference](time.Time) ---

	}
	if target.DeletionTime == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][reference](time.Time) ---
		l, errB := target.DeletionTime.MarshalBinary()
		if errB != nil {
			return errB
		}
		buff.WriteInt(len(l))
		buff.WriteBytes(l)
		// --- [end][write][reference](time.Time) ---

	}
	buff.WriteInt32(target.CpuCores)              // write int32
	buff.WriteInt64(target.RamBytes)              // write int64
	buff.WriteFloat32(target.CpuCost)             // write float32
	buff.WriteFloat32(target.RamCost)             // write float32
	buff.WriteFloat32(target.GpuCost)             // write float32
	buff.WriteFloat32(target.CpuCoreUsageAverage) // write float32
	buff.WriteFloat32(target.CpuCoreUsageMax)     // write float32
	buff.WriteInt64(target.RamBytesUsageAverage)  // write int64
	buff.WriteInt64(target.RamBytesUsageMax)      // write int64
	if target.Diagnostic == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][struct](DiagnosticResult) ---
		buff.WriteInt(0) // [compatibility, unused]
		errC := target.Diagnostic.MarshalBinaryWithContext(ctx)
		if errC != nil {
			return errC
		}
		// --- [end][write][struct](DiagnosticResult) ---

	}
	return nil
}

// UnmarshalBinary uses the data passed byte array to set all the internal properties of
// the Node type
func (target *Node) UnmarshalBinary(data []byte) error {
	var table []string
	buff := util.NewBufferFromBytes(data)

	// string table header validation
	if isBinaryTag(data, BinaryTagStringTable) {
		buff.ReadBytes(len(BinaryTagStringTable)) // strip tag length
		tl := buff.ReadInt()                      // table length
		if tl > 0 {
			table = make([]string, tl, tl)
			for i := 0; i < tl; i++ {
				table[i] = buff.ReadString()
			}
		}
	}

	ctx := &DecodingContext{
		Buffer: buff,
		Table:  table,
	}

	err := target.UnmarshalBinaryWithContext(ctx)
	if err != nil {
		return err
	}

	return nil
}

// UnmarshalBinaryWithContext uses the context containing a string table and binary buffer to set all the internal properties of
// the Node type
func (target *Node) UnmarshalBinaryWithContext(ctx *DecodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	version := buff.ReadUInt8()

	if version > KubemodelCodecVersion {
		return fmt.Errorf("Invalid Version Unmarshaling Node. Expected %d or less, got %d", KubemodelCodecVersion, version)
	}

	var b string
	if ctx.IsStringTable() {
		c := buff.ReadInt() // read string index
		b = ctx.Table[c]
	} else {
		b = buff.ReadString() // read string
	}
	a := b
	target.ID = a

	var e string
	if ctx.IsStringTable() {
		f := buff.ReadInt() // read string index
		e = ctx.Table[f]
	} else {
		e = buff.ReadString() // read string
	}
	d := e
	target.ClusterID = d

	var h string
	if ctx.IsStringTable() {
		k := buff.ReadInt() // read string index
		h = ctx.Table[k]
	} else {
		h = buff.ReadString() // read string
	}
	g := h
	target.ProviderResourceID = g

	var m string
	if ctx.IsStringTable() {
		n := buff.ReadInt() // read string index
		m = ctx.Table[n]
	} else {
		m = buff.ReadString() // read string
	}
	l := m
	target.Name = l

	if buff.ReadUInt8() == uint8(0) {
		target.Labels = nil
	} else {
		// --- [begin][read][map](map[string]string) ---
		p := buff.ReadInt() // map len
		o := make(map[string]string, p)
		for i := 0; i < p; i++ {
			var v string
			var r string
			if ctx.IsStringTable() {
				s := buff.ReadInt() // read string index
				r = ctx.Table[s]
			} else {
				r = buff.ReadString() // read string
			}
			q := r
			v = q

			var z string
			var u string
			if ctx.IsStringTable() {
				w := buff.ReadInt() // read string index
				u = ctx.Table[w]
			} else {
				u = buff.ReadString() // read string
			}
			t := u
			z = t

			o[v] = z
		}
		target.Labels = o
		// --- [end][read][map](map[string]string) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.Annotations = nil
	} else {
		// --- [begin][read][map](map[string]string) ---
		y := buff.ReadInt() // map len
		x := make(map[string]string, y)
		for j := 0; j < y; j++ {
			var vv string
			var bb string
			if ctx.IsStringTable() {
				cc := buff.ReadInt() // read string index
				bb = ctx.Table[cc]
			} else {
				bb = buff.ReadString() // read string
			}
			aa := bb
			vv = aa

			var zz string
			var ee string
			if ctx.IsStringTable() {
				ff := buff.ReadInt() // read string index
				ee = ctx.Table[ff]
			} else {
				ee = buff.ReadString() // read string
			}
			dd := ee
			zz = dd

			x[vv] = zz
		}
		target.Annotations = x
		// --- [end][read][map](map[string]string) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.CreationTime = nil
	} else {
		// --- [begin][read][reference](time.Time) ---
		gg := &time.Time{}
		hh := buff.ReadInt()     // byte array length
		kk := buff.ReadBytes(hh) // byte array
		errA := gg.UnmarshalBinary(kk)
		if errA != nil {
			return errA
		}
		target.CreationTime = gg
		// --- [end][read][reference](time.Time) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.DeletionTime = nil
	} else {
		// --- [begin][read][reference](time.Time) ---
		ll := &time.Time{}
		mm := buff.ReadInt()     // byte array length
		nn := buff.ReadBytes(mm) // byte array
		errB := ll.UnmarshalBinary(nn)
		if errB != nil {
			return errB
		}
		target.DeletionTime = ll
		// --- [end][read][reference](time.Time) ---

	}
	oo := buff.ReadInt32() // read int32
	target.CpuCores = oo

	pp := buff.ReadInt64() // read int64
	target.RamBytes = pp

	qq := buff.ReadFloat32() // read float32
	target.CpuCost = qq

	rr := buff.ReadFloat32() // read float32
	target.RamCost = rr

	ss := buff.ReadFloat32() // read float32
	target.GpuCost = ss

	tt := buff.ReadFloat32() // read float32
	target.CpuCoreUsageAverage = tt

	uu := buff.ReadFloat32() // read float32
	target.CpuCoreUsageMax = uu

	ww := buff.ReadInt64() // read int64
	target.RamBytesUsageAverage = ww

	xx := buff.ReadInt64() // read int64
	target.RamBytesUsageMax = xx

	if buff.ReadUInt8() == uint8(0) {
		target.Diagnostic = nil
	} else {
		// --- [begin][read][struct](DiagnosticResult) ---
		yy := &DiagnosticResult{}
		buff.ReadInt() // [compatibility, unused]
		errC := yy.UnmarshalBinaryWithContext(ctx)
		if errC != nil {
			return errC
		}
		target.Diagnostic = yy
		// --- [end][read][struct](DiagnosticResult) ---

	}
	return nil
}

//--------------------------------------------------------------------------
//  PersistentVolumeClaim
//--------------------------------------------------------------------------

// MarshalBinary serializes the internal properties of this PersistentVolumeClaim instance
// into a byte array
func (target *PersistentVolumeClaim) MarshalBinary() (data []byte, err error) {
	ctx := &EncodingContext{
		Buffer: util.NewBuffer(),
		Table:  nil,
	}

	e := target.MarshalBinaryWithContext(ctx)
	if e != nil {
		return nil, e
	}

	encBytes := ctx.Buffer.Bytes()
	return encBytes, nil
}

// MarshalBinaryWithContext serializes the internal properties of this PersistentVolumeClaim instance
// into a byte array leveraging a predefined context.
func (target *PersistentVolumeClaim) MarshalBinaryWithContext(ctx *EncodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	buff.WriteUInt8(KubemodelCodecVersion) // version

	if ctx.IsStringTable() {
		a := ctx.Table.AddOrGet(target.ID)
		buff.WriteInt(a) // write table index
	} else {
		buff.WriteString(target.ID) // write string
	}
	if ctx.IsStringTable() {
		b := ctx.Table.AddOrGet(target.NamespaceID)
		buff.WriteInt(b) // write table index
	} else {
		buff.WriteString(target.NamespaceID) // write string
	}
	if ctx.IsStringTable() {
		c := ctx.Table.AddOrGet(target.Name)
		buff.WriteInt(c) // write table index
	} else {
		buff.WriteString(target.Name) // write string
	}
	if target.Labels == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][map](map[string]string) ---
		buff.WriteInt(len(target.Labels)) // map length
		for v, z := range target.Labels {
			if ctx.IsStringTable() {
				d := ctx.Table.AddOrGet(v)
				buff.WriteInt(d) // write table index
			} else {
				buff.WriteString(v) // write string
			}
			if ctx.IsStringTable() {
				e := ctx.Table.AddOrGet(z)
				buff.WriteInt(e) // write table index
			} else {
				buff.WriteString(z) // write string
			}
		}
		// --- [end][write][map](map[string]string) ---

	}
	if target.Annotations == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][map](map[string]string) ---
		buff.WriteInt(len(target.Annotations)) // map length
		for vv, zz := range target.Annotations {
			if ctx.IsStringTable() {
				f := ctx.Table.AddOrGet(vv)
				buff.WriteInt(f) // write table index
			} else {
				buff.WriteString(vv) // write string
			}
			if ctx.IsStringTable() {
				g := ctx.Table.AddOrGet(zz)
				buff.WriteInt(g) // write table index
			} else {
				buff.WriteString(zz) // write string
			}
		}
		// --- [end][write][map](map[string]string) ---

	}
	if target.CreationTime == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][reference](time.Time) ---
		h, errA := target.CreationTime.MarshalBinary()
		if errA != nil {
			return errA
		}
		buff.WriteInt(len(h))
		buff.WriteBytes(h)
		// --- [end][write][reference](time.Time) ---

	}
	if target.DeletionTime == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][reference](time.Time) ---
		k, errB := target.DeletionTime.MarshalBinary()
		if errB != nil {
			return errB
		}
		buff.WriteInt(len(k))
		buff.WriteBytes(k)
		// --- [end][write][reference](time.Time) ---

	}
	if ctx.IsStringTable() {
		l := ctx.Table.AddOrGet(target.StorageClass)
		buff.WriteInt(l) // write table index
	} else {
		buff.WriteString(target.StorageClass) // write string
	}
	buff.WriteInt64(target.Size) // write int64
	if ctx.IsStringTable() {
		m := ctx.Table.AddOrGet(target.VolumeName)
		buff.WriteInt(m) // write table index
	} else {
		buff.WriteString(target.VolumeName) // write string
	}
	if target.Diagnostic == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][struct](DiagnosticResult) ---
		buff.WriteInt(0) // [compatibility, unused]
		errC := target.Diagnostic.MarshalBinaryWithContext(ctx)
		if errC != nil {
			return errC
		}
		// --- [end][write][struct](DiagnosticResult) ---

	}
	return nil
}

// UnmarshalBinary uses the data passed byte array to set all the internal properties of
// the PersistentVolumeClaim type
func (target *PersistentVolumeClaim) UnmarshalBinary(data []byte) error {
	var table []string
	buff := util.NewBufferFromBytes(data)

	// string table header validation
	if isBinaryTag(data, BinaryTagStringTable) {
		buff.ReadBytes(len(BinaryTagStringTable)) // strip tag length
		tl := buff.ReadInt()                      // table length
		if tl > 0 {
			table = make([]string, tl, tl)
			for i := 0; i < tl; i++ {
				table[i] = buff.ReadString()
			}
		}
	}

	ctx := &DecodingContext{
		Buffer: buff,
		Table:  table,
	}

	err := target.UnmarshalBinaryWithContext(ctx)
	if err != nil {
		return err
	}

	return nil
}

// UnmarshalBinaryWithContext uses the context containing a string table and binary buffer to set all the internal properties of
// the PersistentVolumeClaim type
func (target *PersistentVolumeClaim) UnmarshalBinaryWithContext(ctx *DecodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	version := buff.ReadUInt8()

	if version > KubemodelCodecVersion {
		return fmt.Errorf("Invalid Version Unmarshaling PersistentVolumeClaim. Expected %d or less, got %d", KubemodelCodecVersion, version)
	}

	var b string
	if ctx.IsStringTable() {
		c := buff.ReadInt() // read string index
		b = ctx.Table[c]
	} else {
		b = buff.ReadString() // read string
	}
	a := b
	target.ID = a

	var e string
	if ctx.IsStringTable() {
		f := buff.ReadInt() // read string index
		e = ctx.Table[f]
	} else {
		e = buff.ReadString() // read string
	}
	d := e
	target.NamespaceID = d

	var h string
	if ctx.IsStringTable() {
		k := buff.ReadInt() // read string index
		h = ctx.Table[k]
	} else {
		h = buff.ReadString() // read string
	}
	g := h
	target.Name = g

	if buff.ReadUInt8() == uint8(0) {
		target.Labels = nil
	} else {
		// --- [begin][read][map](map[string]string) ---
		m := buff.ReadInt() // map len
		l := make(map[string]string, m)
		for i := 0; i < m; i++ {
			var v string
			var o string
			if ctx.IsStringTable() {
				p := buff.ReadInt() // read string index
				o = ctx.Table[p]
			} else {
				o = buff.ReadString() // read string
			}
			n := o
			v = n

			var z string
			var r string
			if ctx.IsStringTable() {
				s := buff.ReadInt() // read string index
				r = ctx.Table[s]
			} else {
				r = buff.ReadString() // read string
			}
			q := r
			z = q

			l[v] = z
		}
		target.Labels = l
		// --- [end][read][map](map[string]string) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.Annotations = nil
	} else {
		// --- [begin][read][map](map[string]string) ---
		u := buff.ReadInt() // map len
		t := make(map[string]string, u)
		for j := 0; j < u; j++ {
			var vv string
			var x string
			if ctx.IsStringTable() {
				y := buff.ReadInt() // read string index
				x = ctx.Table[y]
			} else {
				x = buff.ReadString() // read string
			}
			w := x
			vv = w

			var zz string
			var bb string
			if ctx.IsStringTable() {
				cc := buff.ReadInt() // read string index
				bb = ctx.Table[cc]
			} else {
				bb = buff.ReadString() // read string
			}
			aa := bb
			zz = aa

			t[vv] = zz
		}
		target.Annotations = t
		// --- [end][read][map](map[string]string) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.CreationTime = nil
	} else {
		// --- [begin][read][reference](time.Time) ---
		dd := &time.Time{}
		ee := buff.ReadInt()     // byte array length
		ff := buff.ReadBytes(ee) // byte array
		errA := dd.UnmarshalBinary(ff)
		if errA != nil {
			return errA
		}
		target.CreationTime = dd
		// --- [end][read][reference](time.Time) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.DeletionTime = nil
	} else {
		// --- [begin][read][reference](time.Time) ---
		gg := &time.Time{}
		hh := buff.ReadInt()     // byte array length
		kk := buff.ReadBytes(hh) // byte array
		errB := gg.UnmarshalBinary(kk)
		if errB != nil {
			return errB
		}
		target.DeletionTime = gg
		// --- [end][read][reference](time.Time) ---

	}
	var mm string
	if ctx.IsStringTable() {
		nn := buff.ReadInt() // read string index
		mm = ctx.Table[nn]
	} else {
		mm = buff.ReadString() // read string
	}
	ll := mm
	target.StorageClass = ll

	oo := buff.ReadInt64() // read int64
	target.Size = oo

	var qq string
	if ctx.IsStringTable() {
		rr := buff.ReadInt() // read string index
		qq = ctx.Table[rr]
	} else {
		qq = buff.ReadString() // read string
	}
	pp := qq
	target.VolumeName = pp

	if buff.ReadUInt8() == uint8(0) {
		target.Diagnostic = nil
	} else {
		// --- [begin][read][struct](DiagnosticResult) ---
		ss := &DiagnosticResult{}
		buff.ReadInt() // [compatibility, unused]
		errC := ss.UnmarshalBinaryWithContext(ctx)
		if errC != nil {
			return errC
		}
		target.Diagnostic = ss
		// --- [end][read][struct](DiagnosticResult) ---

	}
	return nil
}

//--------------------------------------------------------------------------
//  Pod
//--------------------------------------------------------------------------

// MarshalBinary serializes the internal properties of this Pod instance
// into a byte array
func (target *Pod) MarshalBinary() (data []byte, err error) {
	ctx := &EncodingContext{
		Buffer: util.NewBuffer(),
		Table:  nil,
	}

	e := target.MarshalBinaryWithContext(ctx)
	if e != nil {
		return nil, e
	}

	encBytes := ctx.Buffer.Bytes()
	return encBytes, nil
}

// MarshalBinaryWithContext serializes the internal properties of this Pod instance
// into a byte array leveraging a predefined context.
func (target *Pod) MarshalBinaryWithContext(ctx *EncodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	buff.WriteUInt8(KubemodelCodecVersion) // version

	if ctx.IsStringTable() {
		a := ctx.Table.AddOrGet(target.ID)
		buff.WriteInt(a) // write table index
	} else {
		buff.WriteString(target.ID) // write string
	}
	if ctx.IsStringTable() {
		b := ctx.Table.AddOrGet(target.NamespaceID)
		buff.WriteInt(b) // write table index
	} else {
		buff.WriteString(target.NamespaceID) // write string
	}
	if ctx.IsStringTable() {
		c := ctx.Table.AddOrGet(target.ControllerID)
		buff.WriteInt(c) // write table index
	} else {
		buff.WriteString(target.ControllerID) // write string
	}
	if ctx.IsStringTable() {
		d := ctx.Table.AddOrGet(target.NodeID)
		buff.WriteInt(d) // write table index
	} else {
		buff.WriteString(target.NodeID) // write string
	}
	if ctx.IsStringTable() {
		e := ctx.Table.AddOrGet(target.Name)
		buff.WriteInt(e) // write table index
	} else {
		buff.WriteString(target.Name) // write string
	}
	if target.Labels == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][map](map[string]string) ---
		buff.WriteInt(len(target.Labels)) // map length
		for v, z := range target.Labels {
			if ctx.IsStringTable() {
				f := ctx.Table.AddOrGet(v)
				buff.WriteInt(f) // write table index
			} else {
				buff.WriteString(v) // write string
			}
			if ctx.IsStringTable() {
				g := ctx.Table.AddOrGet(z)
				buff.WriteInt(g) // write table index
			} else {
				buff.WriteString(z) // write string
			}
		}
		// --- [end][write][map](map[string]string) ---

	}
	if target.Annotations == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][map](map[string]string) ---
		buff.WriteInt(len(target.Annotations)) // map length
		for vv, zz := range target.Annotations {
			if ctx.IsStringTable() {
				h := ctx.Table.AddOrGet(vv)
				buff.WriteInt(h) // write table index
			} else {
				buff.WriteString(vv) // write string
			}
			if ctx.IsStringTable() {
				k := ctx.Table.AddOrGet(zz)
				buff.WriteInt(k) // write table index
			} else {
				buff.WriteString(zz) // write string
			}
		}
		// --- [end][write][map](map[string]string) ---

	}
	if target.CreationTime == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][reference](time.Time) ---
		l, errA := target.CreationTime.MarshalBinary()
		if errA != nil {
			return errA
		}
		buff.WriteInt(len(l))
		buff.WriteBytes(l)
		// --- [end][write][reference](time.Time) ---

	}
	if target.DeletionTime == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][reference](time.Time) ---
		m, errB := target.DeletionTime.MarshalBinary()
		if errB != nil {
			return errB
		}
		buff.WriteInt(len(m))
		buff.WriteBytes(m)
		// --- [end][write][reference](time.Time) ---

	}
	buff.WriteFloat32(target.CpuCoreHours)          // write float32
	buff.WriteFloat32(target.CpuCoreRequestAverage) // write float32
	buff.WriteFloat32(target.CpuCoreUsageAverage)   // write float32
	buff.WriteFloat32(target.CpuCoreUsageMax)       // write float32
	buff.WriteInt64(target.RamByteHours)            // write int64
	buff.WriteInt64(target.RamBytesRequestAverage)  // write int64
	buff.WriteInt64(target.RamBytesUsageAverage)    // write int64
	buff.WriteInt64(target.RamBytesUsageMax)        // write int64
	buff.WriteInt64(target.StorageByteHours)        // write int64
	buff.WriteInt64(target.NetworkTransferBytes)    // write int64
	buff.WriteInt64(target.NetworkReceiveBytes)     // write int64
	if target.Diagnostic == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][struct](DiagnosticResult) ---
		buff.WriteInt(0) // [compatibility, unused]
		errC := target.Diagnostic.MarshalBinaryWithContext(ctx)
		if errC != nil {
			return errC
		}
		// --- [end][write][struct](DiagnosticResult) ---

	}
	return nil
}

// UnmarshalBinary uses the data passed byte array to set all the internal properties of
// the Pod type
func (target *Pod) UnmarshalBinary(data []byte) error {
	var table []string
	buff := util.NewBufferFromBytes(data)

	// string table header validation
	if isBinaryTag(data, BinaryTagStringTable) {
		buff.ReadBytes(len(BinaryTagStringTable)) // strip tag length
		tl := buff.ReadInt()                      // table length
		if tl > 0 {
			table = make([]string, tl, tl)
			for i := 0; i < tl; i++ {
				table[i] = buff.ReadString()
			}
		}
	}

	ctx := &DecodingContext{
		Buffer: buff,
		Table:  table,
	}

	err := target.UnmarshalBinaryWithContext(ctx)
	if err != nil {
		return err
	}

	return nil
}

// UnmarshalBinaryWithContext uses the context containing a string table and binary buffer to set all the internal properties of
// the Pod type
func (target *Pod) UnmarshalBinaryWithContext(ctx *DecodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	version := buff.ReadUInt8()

	if version > KubemodelCodecVersion {
		return fmt.Errorf("Invalid Version Unmarshaling Pod. Expected %d or less, got %d", KubemodelCodecVersion, version)
	}

	var b string
	if ctx.IsStringTable() {
		c := buff.ReadInt() // read string index
		b = ctx.Table[c]
	} else {
		b = buff.ReadString() // read string
	}
	a := b
	target.ID = a

	var e string
	if ctx.IsStringTable() {
		f := buff.ReadInt() // read string index
		e = ctx.Table[f]
	} else {
		e = buff.ReadString() // read string
	}
	d := e
	target.NamespaceID = d

	var h string
	if ctx.IsStringTable() {
		k := buff.ReadInt() // read string index
		h = ctx.Table[k]
	} else {
		h = buff.ReadString() // read string
	}
	g := h
	target.ControllerID = g

	var m string
	if ctx.IsStringTable() {
		n := buff.ReadInt() // read string index
		m = ctx.Table[n]
	} else {
		m = buff.ReadString() // read string
	}
	l := m
	target.NodeID = l

	var p string
	if ctx.IsStringTable() {
		q := buff.ReadInt() // read string index
		p = ctx.Table[q]
	} else {
		p = buff.ReadString() // read string
	}
	o := p
	target.Name = o

	if buff.ReadUInt8() == uint8(0) {
		target.Labels = nil
	} else {
		// --- [begin][read][map](map[string]string) ---
		s := buff.ReadInt() // map len
		r := make(map[string]string, s)
		for i := 0; i < s; i++ {
			var v string
			var u string
			if ctx.IsStringTable() {
				w := buff.ReadInt() // read string index
				u = ctx.Table[w]
			} else {
				u = buff.ReadString() // read string
			}
			t := u
			v = t

			var z string
			var y string
			if ctx.IsStringTable() {
				aa := buff.ReadInt() // read string index
				y = ctx.Table[aa]
			} else {
				y = buff.ReadString() // read string
			}
			x := y
			z = x

			r[v] = z
		}
		target.Labels = r
		// --- [end][read][map](map[string]string) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.Annotations = nil
	} else {
		// --- [begin][read][map](map[string]string) ---
		cc := buff.ReadInt() // map len
		bb := make(map[string]string, cc)
		for j := 0; j < cc; j++ {
			var vv string
			var ee string
			if ctx.IsStringTable() {
				ff := buff.ReadInt() // read string index
				ee = ctx.Table[ff]
			} else {
				ee = buff.ReadString() // read string
			}
			dd := ee
			vv = dd

			var zz string
			var hh string
			if ctx.IsStringTable() {
				kk := buff.ReadInt() // read string index
				hh = ctx.Table[kk]
			} else {
				hh = buff.ReadString() // read string
			}
			gg := hh
			zz = gg

			bb[vv] = zz
		}
		target.Annotations = bb
		// --- [end][read][map](map[string]string) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.CreationTime = nil
	} else {
		// --- [begin][read][reference](time.Time) ---
		ll := &time.Time{}
		mm := buff.ReadInt()     // byte array length
		nn := buff.ReadBytes(mm) // byte array
		errA := ll.UnmarshalBinary(nn)
		if errA != nil {
			return errA
		}
		target.CreationTime = ll
		// --- [end][read][reference](time.Time) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.DeletionTime = nil
	} else {
		// --- [begin][read][reference](time.Time) ---
		oo := &time.Time{}
		pp := buff.ReadInt()     // byte array length
		qq := buff.ReadBytes(pp) // byte array
		errB := oo.UnmarshalBinary(qq)
		if errB != nil {
			return errB
		}
		target.DeletionTime = oo
		// --- [end][read][reference](time.Time) ---

	}
	rr := buff.ReadFloat32() // read float32
	target.CpuCoreHours = rr

	ss := buff.ReadFloat32() // read float32
	target.CpuCoreRequestAverage = ss

	tt := buff.ReadFloat32() // read float32
	target.CpuCoreUsageAverage = tt

	uu := buff.ReadFloat32() // read float32
	target.CpuCoreUsageMax = uu

	ww := buff.ReadInt64() // read int64
	target.RamByteHours = ww

	xx := buff.ReadInt64() // read int64
	target.RamBytesRequestAverage = xx

	yy := buff.ReadInt64() // read int64
	target.RamBytesUsageAverage = yy

	aaa := buff.ReadInt64() // read int64
	target.RamBytesUsageMax = aaa

	bbb := buff.ReadInt64() // read int64
	target.StorageByteHours = bbb

	ccc := buff.ReadInt64() // read int64
	target.NetworkTransferBytes = ccc

	ddd := buff.ReadInt64() // read int64
	target.NetworkReceiveBytes = ddd

	if buff.ReadUInt8() == uint8(0) {
		target.Diagnostic = nil
	} else {
		// --- [begin][read][struct](DiagnosticResult) ---
		eee := &DiagnosticResult{}
		buff.ReadInt() // [compatibility, unused]
		errC := eee.UnmarshalBinaryWithContext(ctx)
		if errC != nil {
			return errC
		}
		target.Diagnostic = eee
		// --- [end][read][struct](DiagnosticResult) ---

	}
	return nil
}

//--------------------------------------------------------------------------
//  Service
//--------------------------------------------------------------------------

// MarshalBinary serializes the internal properties of this Service instance
// into a byte array
func (target *Service) MarshalBinary() (data []byte, err error) {
	ctx := &EncodingContext{
		Buffer: util.NewBuffer(),
		Table:  nil,
	}

	e := target.MarshalBinaryWithContext(ctx)
	if e != nil {
		return nil, e
	}

	encBytes := ctx.Buffer.Bytes()
	return encBytes, nil
}

// MarshalBinaryWithContext serializes the internal properties of this Service instance
// into a byte array leveraging a predefined context.
func (target *Service) MarshalBinaryWithContext(ctx *EncodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	buff.WriteUInt8(KubemodelCodecVersion) // version

	if ctx.IsStringTable() {
		a := ctx.Table.AddOrGet(target.ID)
		buff.WriteInt(a) // write table index
	} else {
		buff.WriteString(target.ID) // write string
	}
	if ctx.IsStringTable() {
		b := ctx.Table.AddOrGet(target.ClusterID)
		buff.WriteInt(b) // write table index
	} else {
		buff.WriteString(target.ClusterID) // write string
	}
	if ctx.IsStringTable() {
		c := ctx.Table.AddOrGet(target.NamespaceID)
		buff.WriteInt(c) // write table index
	} else {
		buff.WriteString(target.NamespaceID) // write string
	}
	if ctx.IsStringTable() {
		d := ctx.Table.AddOrGet(target.Name)
		buff.WriteInt(d) // write table index
	} else {
		buff.WriteString(target.Name) // write string
	}
	if ctx.IsStringTable() {
		e := ctx.Table.AddOrGet(target.Type)
		buff.WriteInt(e) // write table index
	} else {
		buff.WriteString(target.Type) // write string
	}
	if target.Labels == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][map](map[string]string) ---
		buff.WriteInt(len(target.Labels)) // map length
		for v, z := range target.Labels {
			if ctx.IsStringTable() {
				f := ctx.Table.AddOrGet(v)
				buff.WriteInt(f) // write table index
			} else {
				buff.WriteString(v) // write string
			}
			if ctx.IsStringTable() {
				g := ctx.Table.AddOrGet(z)
				buff.WriteInt(g) // write table index
			} else {
				buff.WriteString(z) // write string
			}
		}
		// --- [end][write][map](map[string]string) ---

	}
	if target.Annotations == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][map](map[string]string) ---
		buff.WriteInt(len(target.Annotations)) // map length
		for vv, zz := range target.Annotations {
			if ctx.IsStringTable() {
				h := ctx.Table.AddOrGet(vv)
				buff.WriteInt(h) // write table index
			} else {
				buff.WriteString(vv) // write string
			}
			if ctx.IsStringTable() {
				k := ctx.Table.AddOrGet(zz)
				buff.WriteInt(k) // write table index
			} else {
				buff.WriteString(zz) // write string
			}
		}
		// --- [end][write][map](map[string]string) ---

	}
	if target.Ports == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][slice]([]ServicePort) ---
		buff.WriteInt(len(target.Ports)) // array length
		for i := 0; i < len(target.Ports); i++ {
			// --- [begin][write][struct](ServicePort) ---
			buff.WriteInt(0) // [compatibility, unused]
			errA := target.Ports[i].MarshalBinaryWithContext(ctx)
			if errA != nil {
				return errA
			}
			// --- [end][write][struct](ServicePort) ---

		}
		// --- [end][write][slice]([]ServicePort) ---

	}
	if target.CreationTime == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][reference](time.Time) ---
		l, errB := target.CreationTime.MarshalBinary()
		if errB != nil {
			return errB
		}
		buff.WriteInt(len(l))
		buff.WriteBytes(l)
		// --- [end][write][reference](time.Time) ---

	}
	if target.DeletionTime == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][reference](time.Time) ---
		m, errC := target.DeletionTime.MarshalBinary()
		if errC != nil {
			return errC
		}
		buff.WriteInt(len(m))
		buff.WriteBytes(m)
		// --- [end][write][reference](time.Time) ---

	}
	if target.Diagnostic == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][struct](DiagnosticResult) ---
		buff.WriteInt(0) // [compatibility, unused]
		errD := target.Diagnostic.MarshalBinaryWithContext(ctx)
		if errD != nil {
			return errD
		}
		// --- [end][write][struct](DiagnosticResult) ---

	}
	return nil
}

// UnmarshalBinary uses the data passed byte array to set all the internal properties of
// the Service type
func (target *Service) UnmarshalBinary(data []byte) error {
	var table []string
	buff := util.NewBufferFromBytes(data)

	// string table header validation
	if isBinaryTag(data, BinaryTagStringTable) {
		buff.ReadBytes(len(BinaryTagStringTable)) // strip tag length
		tl := buff.ReadInt()                      // table length
		if tl > 0 {
			table = make([]string, tl, tl)
			for i := 0; i < tl; i++ {
				table[i] = buff.ReadString()
			}
		}
	}

	ctx := &DecodingContext{
		Buffer: buff,
		Table:  table,
	}

	err := target.UnmarshalBinaryWithContext(ctx)
	if err != nil {
		return err
	}

	return nil
}

// UnmarshalBinaryWithContext uses the context containing a string table and binary buffer to set all the internal properties of
// the Service type
func (target *Service) UnmarshalBinaryWithContext(ctx *DecodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	version := buff.ReadUInt8()

	if version > KubemodelCodecVersion {
		return fmt.Errorf("Invalid Version Unmarshaling Service. Expected %d or less, got %d", KubemodelCodecVersion, version)
	}

	var b string
	if ctx.IsStringTable() {
		c := buff.ReadInt() // read string index
		b = ctx.Table[c]
	} else {
		b = buff.ReadString() // read string
	}
	a := b
	target.ID = a

	var e string
	if ctx.IsStringTable() {
		f := buff.ReadInt() // read string index
		e = ctx.Table[f]
	} else {
		e = buff.ReadString() // read string
	}
	d := e
	target.ClusterID = d

	var h string
	if ctx.IsStringTable() {
		k := buff.ReadInt() // read string index
		h = ctx.Table[k]
	} else {
		h = buff.ReadString() // read string
	}
	g := h
	target.NamespaceID = g

	var m string
	if ctx.IsStringTable() {
		n := buff.ReadInt() // read string index
		m = ctx.Table[n]
	} else {
		m = buff.ReadString() // read string
	}
	l := m
	target.Name = l

	var p string
	if ctx.IsStringTable() {
		q := buff.ReadInt() // read string index
		p = ctx.Table[q]
	} else {
		p = buff.ReadString() // read string
	}
	o := p
	target.Type = o

	if buff.ReadUInt8() == uint8(0) {
		target.Labels = nil
	} else {
		// --- [begin][read][map](map[string]string) ---
		s := buff.ReadInt() // map len
		r := make(map[string]string, s)
		for i := 0; i < s; i++ {
			var v string
			var u string
			if ctx.IsStringTable() {
				w := buff.ReadInt() // read string index
				u = ctx.Table[w]
			} else {
				u = buff.ReadString() // read string
			}
			t := u
			v = t

			var z string
			var y string
			if ctx.IsStringTable() {
				aa := buff.ReadInt() // read string index
				y = ctx.Table[aa]
			} else {
				y = buff.ReadString() // read string
			}
			x := y
			z = x

			r[v] = z
		}
		target.Labels = r
		// --- [end][read][map](map[string]string) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.Annotations = nil
	} else {
		// --- [begin][read][map](map[string]string) ---
		cc := buff.ReadInt() // map len
		bb := make(map[string]string, cc)
		for j := 0; j < cc; j++ {
			var vv string
			var ee string
			if ctx.IsStringTable() {
				ff := buff.ReadInt() // read string index
				ee = ctx.Table[ff]
			} else {
				ee = buff.ReadString() // read string
			}
			dd := ee
			vv = dd

			var zz string
			var hh string
			if ctx.IsStringTable() {
				kk := buff.ReadInt() // read string index
				hh = ctx.Table[kk]
			} else {
				hh = buff.ReadString() // read string
			}
			gg := hh
			zz = gg

			bb[vv] = zz
		}
		target.Annotations = bb
		// --- [end][read][map](map[string]string) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.Ports = nil
	} else {
		// --- [begin][read][slice]([]ServicePort) ---
		mm := buff.ReadInt() // array len
		ll := make([]ServicePort, mm)
		for ii := 0; ii < mm; ii++ {
			// --- [begin][read][struct](ServicePort) ---
			oo := &ServicePort{}
			buff.ReadInt() // [compatibility, unused]
			errA := oo.UnmarshalBinaryWithContext(ctx)
			if errA != nil {
				return errA
			}
			nn := *oo
			// --- [end][read][struct](ServicePort) ---

			ll[ii] = nn
		}
		target.Ports = ll
		// --- [end][read][slice]([]ServicePort) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.CreationTime = nil
	} else {
		// --- [begin][read][reference](time.Time) ---
		pp := &time.Time{}
		qq := buff.ReadInt()     // byte array length
		rr := buff.ReadBytes(qq) // byte array
		errB := pp.UnmarshalBinary(rr)
		if errB != nil {
			return errB
		}
		target.CreationTime = pp
		// --- [end][read][reference](time.Time) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.DeletionTime = nil
	} else {
		// --- [begin][read][reference](time.Time) ---
		ss := &time.Time{}
		tt := buff.ReadInt()     // byte array length
		uu := buff.ReadBytes(tt) // byte array
		errC := ss.UnmarshalBinary(uu)
		if errC != nil {
			return errC
		}
		target.DeletionTime = ss
		// --- [end][read][reference](time.Time) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.Diagnostic = nil
	} else {
		// --- [begin][read][struct](DiagnosticResult) ---
		ww := &DiagnosticResult{}
		buff.ReadInt() // [compatibility, unused]
		errD := ww.UnmarshalBinaryWithContext(ctx)
		if errD != nil {
			return errD
		}
		target.Diagnostic = ww
		// --- [end][read][struct](DiagnosticResult) ---

	}
	return nil
}

//--------------------------------------------------------------------------
//  ServicePort
//--------------------------------------------------------------------------

// MarshalBinary serializes the internal properties of this ServicePort instance
// into a byte array
func (target *ServicePort) MarshalBinary() (data []byte, err error) {
	ctx := &EncodingContext{
		Buffer: util.NewBuffer(),
		Table:  nil,
	}

	e := target.MarshalBinaryWithContext(ctx)
	if e != nil {
		return nil, e
	}

	encBytes := ctx.Buffer.Bytes()
	return encBytes, nil
}

// MarshalBinaryWithContext serializes the internal properties of this ServicePort instance
// into a byte array leveraging a predefined context.
func (target *ServicePort) MarshalBinaryWithContext(ctx *EncodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	buff.WriteUInt8(KubemodelCodecVersion) // version

	if ctx.IsStringTable() {
		a := ctx.Table.AddOrGet(target.Name)
		buff.WriteInt(a) // write table index
	} else {
		buff.WriteString(target.Name) // write string
	}
	buff.WriteInt32(target.Port)       // write int32
	buff.WriteInt32(target.TargetPort) // write int32
	if ctx.IsStringTable() {
		b := ctx.Table.AddOrGet(target.Protocol)
		buff.WriteInt(b) // write table index
	} else {
		buff.WriteString(target.Protocol) // write string
	}
	return nil
}

// UnmarshalBinary uses the data passed byte array to set all the internal properties of
// the ServicePort type
func (target *ServicePort) UnmarshalBinary(data []byte) error {
	var table []string
	buff := util.NewBufferFromBytes(data)

	// string table header validation
	if isBinaryTag(data, BinaryTagStringTable) {
		buff.ReadBytes(len(BinaryTagStringTable)) // strip tag length
		tl := buff.ReadInt()                      // table length
		if tl > 0 {
			table = make([]string, tl, tl)
			for i := 0; i < tl; i++ {
				table[i] = buff.ReadString()
			}
		}
	}

	ctx := &DecodingContext{
		Buffer: buff,
		Table:  table,
	}

	err := target.UnmarshalBinaryWithContext(ctx)
	if err != nil {
		return err
	}

	return nil
}

// UnmarshalBinaryWithContext uses the context containing a string table and binary buffer to set all the internal properties of
// the ServicePort type
func (target *ServicePort) UnmarshalBinaryWithContext(ctx *DecodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	version := buff.ReadUInt8()

	if version > KubemodelCodecVersion {
		return fmt.Errorf("Invalid Version Unmarshaling ServicePort. Expected %d or less, got %d", KubemodelCodecVersion, version)
	}

	var b string
	if ctx.IsStringTable() {
		c := buff.ReadInt() // read string index
		b = ctx.Table[c]
	} else {
		b = buff.ReadString() // read string
	}
	a := b
	target.Name = a

	d := buff.ReadInt32() // read int32
	target.Port = d

	e := buff.ReadInt32() // read int32
	target.TargetPort = e

	var g string
	if ctx.IsStringTable() {
		h := buff.ReadInt() // read string index
		g = ctx.Table[h]
	} else {
		g = buff.ReadString() // read string
	}
	f := g
	target.Protocol = f

	return nil
}

//--------------------------------------------------------------------------
//  Volume
//--------------------------------------------------------------------------

// MarshalBinary serializes the internal properties of this Volume instance
// into a byte array
func (target *Volume) MarshalBinary() (data []byte, err error) {
	ctx := &EncodingContext{
		Buffer: util.NewBuffer(),
		Table:  nil,
	}

	e := target.MarshalBinaryWithContext(ctx)
	if e != nil {
		return nil, e
	}

	encBytes := ctx.Buffer.Bytes()
	return encBytes, nil
}

// MarshalBinaryWithContext serializes the internal properties of this Volume instance
// into a byte array leveraging a predefined context.
func (target *Volume) MarshalBinaryWithContext(ctx *EncodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	buff.WriteUInt8(KubemodelCodecVersion) // version

	if ctx.IsStringTable() {
		a := ctx.Table.AddOrGet(target.ID)
		buff.WriteInt(a) // write table index
	} else {
		buff.WriteString(target.ID) // write string
	}
	if ctx.IsStringTable() {
		b := ctx.Table.AddOrGet(target.ClusterID)
		buff.WriteInt(b) // write table index
	} else {
		buff.WriteString(target.ClusterID) // write string
	}
	if ctx.IsStringTable() {
		c := ctx.Table.AddOrGet(target.Name)
		buff.WriteInt(c) // write table index
	} else {
		buff.WriteString(target.Name) // write string
	}
	if ctx.IsStringTable() {
		d := ctx.Table.AddOrGet(target.Namespace)
		buff.WriteInt(d) // write table index
	} else {
		buff.WriteString(target.Namespace) // write string
	}
	if target.Labels == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][map](map[string]string) ---
		buff.WriteInt(len(target.Labels)) // map length
		for v, z := range target.Labels {
			if ctx.IsStringTable() {
				e := ctx.Table.AddOrGet(v)
				buff.WriteInt(e) // write table index
			} else {
				buff.WriteString(v) // write string
			}
			if ctx.IsStringTable() {
				f := ctx.Table.AddOrGet(z)
				buff.WriteInt(f) // write table index
			} else {
				buff.WriteString(z) // write string
			}
		}
		// --- [end][write][map](map[string]string) ---

	}
	if target.Annotations == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][map](map[string]string) ---
		buff.WriteInt(len(target.Annotations)) // map length
		for vv, zz := range target.Annotations {
			if ctx.IsStringTable() {
				g := ctx.Table.AddOrGet(vv)
				buff.WriteInt(g) // write table index
			} else {
				buff.WriteString(vv) // write string
			}
			if ctx.IsStringTable() {
				h := ctx.Table.AddOrGet(zz)
				buff.WriteInt(h) // write table index
			} else {
				buff.WriteString(zz) // write string
			}
		}
		// --- [end][write][map](map[string]string) ---

	}
	if target.CreationTime == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][reference](time.Time) ---
		k, errA := target.CreationTime.MarshalBinary()
		if errA != nil {
			return errA
		}
		buff.WriteInt(len(k))
		buff.WriteBytes(k)
		// --- [end][write][reference](time.Time) ---

	}
	if target.DeletionTime == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][reference](time.Time) ---
		l, errB := target.DeletionTime.MarshalBinary()
		if errB != nil {
			return errB
		}
		buff.WriteInt(len(l))
		buff.WriteBytes(l)
		// --- [end][write][reference](time.Time) ---

	}
	if ctx.IsStringTable() {
		m := ctx.Table.AddOrGet(target.StorageClass)
		buff.WriteInt(m) // write table index
	} else {
		buff.WriteString(target.StorageClass) // write string
	}
	buff.WriteInt64(target.Size)   // write int64
	buff.WriteFloat32(target.Cost) // write float32
	if target.Diagnostic == nil {
		buff.WriteUInt8(uint8(0)) // write nil byte
	} else {
		buff.WriteUInt8(uint8(1)) // write non-nil byte

		// --- [begin][write][struct](DiagnosticResult) ---
		buff.WriteInt(0) // [compatibility, unused]
		errC := target.Diagnostic.MarshalBinaryWithContext(ctx)
		if errC != nil {
			return errC
		}
		// --- [end][write][struct](DiagnosticResult) ---

	}
	return nil
}

// UnmarshalBinary uses the data passed byte array to set all the internal properties of
// the Volume type
func (target *Volume) UnmarshalBinary(data []byte) error {
	var table []string
	buff := util.NewBufferFromBytes(data)

	// string table header validation
	if isBinaryTag(data, BinaryTagStringTable) {
		buff.ReadBytes(len(BinaryTagStringTable)) // strip tag length
		tl := buff.ReadInt()                      // table length
		if tl > 0 {
			table = make([]string, tl, tl)
			for i := 0; i < tl; i++ {
				table[i] = buff.ReadString()
			}
		}
	}

	ctx := &DecodingContext{
		Buffer: buff,
		Table:  table,
	}

	err := target.UnmarshalBinaryWithContext(ctx)
	if err != nil {
		return err
	}

	return nil
}

// UnmarshalBinaryWithContext uses the context containing a string table and binary buffer to set all the internal properties of
// the Volume type
func (target *Volume) UnmarshalBinaryWithContext(ctx *DecodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	version := buff.ReadUInt8()

	if version > KubemodelCodecVersion {
		return fmt.Errorf("Invalid Version Unmarshaling Volume. Expected %d or less, got %d", KubemodelCodecVersion, version)
	}

	var b string
	if ctx.IsStringTable() {
		c := buff.ReadInt() // read string index
		b = ctx.Table[c]
	} else {
		b = buff.ReadString() // read string
	}
	a := b
	target.ID = a

	var e string
	if ctx.IsStringTable() {
		f := buff.ReadInt() // read string index
		e = ctx.Table[f]
	} else {
		e = buff.ReadString() // read string
	}
	d := e
	target.ClusterID = d

	var h string
	if ctx.IsStringTable() {
		k := buff.ReadInt() // read string index
		h = ctx.Table[k]
	} else {
		h = buff.ReadString() // read string
	}
	g := h
	target.Name = g

	var m string
	if ctx.IsStringTable() {
		n := buff.ReadInt() // read string index
		m = ctx.Table[n]
	} else {
		m = buff.ReadString() // read string
	}
	l := m
	target.Namespace = l

	if buff.ReadUInt8() == uint8(0) {
		target.Labels = nil
	} else {
		// --- [begin][read][map](map[string]string) ---
		p := buff.ReadInt() // map len
		o := make(map[string]string, p)
		for i := 0; i < p; i++ {
			var v string
			var r string
			if ctx.IsStringTable() {
				s := buff.ReadInt() // read string index
				r = ctx.Table[s]
			} else {
				r = buff.ReadString() // read string
			}
			q := r
			v = q

			var z string
			var u string
			if ctx.IsStringTable() {
				w := buff.ReadInt() // read string index
				u = ctx.Table[w]
			} else {
				u = buff.ReadString() // read string
			}
			t := u
			z = t

			o[v] = z
		}
		target.Labels = o
		// --- [end][read][map](map[string]string) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.Annotations = nil
	} else {
		// --- [begin][read][map](map[string]string) ---
		y := buff.ReadInt() // map len
		x := make(map[string]string, y)
		for j := 0; j < y; j++ {
			var vv string
			var bb string
			if ctx.IsStringTable() {
				cc := buff.ReadInt() // read string index
				bb = ctx.Table[cc]
			} else {
				bb = buff.ReadString() // read string
			}
			aa := bb
			vv = aa

			var zz string
			var ee string
			if ctx.IsStringTable() {
				ff := buff.ReadInt() // read string index
				ee = ctx.Table[ff]
			} else {
				ee = buff.ReadString() // read string
			}
			dd := ee
			zz = dd

			x[vv] = zz
		}
		target.Annotations = x
		// --- [end][read][map](map[string]string) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.CreationTime = nil
	} else {
		// --- [begin][read][reference](time.Time) ---
		gg := &time.Time{}
		hh := buff.ReadInt()     // byte array length
		kk := buff.ReadBytes(hh) // byte array
		errA := gg.UnmarshalBinary(kk)
		if errA != nil {
			return errA
		}
		target.CreationTime = gg
		// --- [end][read][reference](time.Time) ---

	}
	if buff.ReadUInt8() == uint8(0) {
		target.DeletionTime = nil
	} else {
		// --- [begin][read][reference](time.Time) ---
		ll := &time.Time{}
		mm := buff.ReadInt()     // byte array length
		nn := buff.ReadBytes(mm) // byte array
		errB := ll.UnmarshalBinary(nn)
		if errB != nil {
			return errB
		}
		target.DeletionTime = ll
		// --- [end][read][reference](time.Time) ---

	}
	var pp string
	if ctx.IsStringTable() {
		qq := buff.ReadInt() // read string index
		pp = ctx.Table[qq]
	} else {
		pp = buff.ReadString() // read string
	}
	oo := pp
	target.StorageClass = oo

	rr := buff.ReadInt64() // read int64
	target.Size = rr

	ss := buff.ReadFloat32() // read float32
	target.Cost = ss

	if buff.ReadUInt8() == uint8(0) {
		target.Diagnostic = nil
	} else {
		// --- [begin][read][struct](DiagnosticResult) ---
		tt := &DiagnosticResult{}
		buff.ReadInt() // [compatibility, unused]
		errC := tt.UnmarshalBinaryWithContext(ctx)
		if errC != nil {
			return errC
		}
		target.Diagnostic = tt
		// --- [end][read][struct](DiagnosticResult) ---

	}
	return nil
}

//--------------------------------------------------------------------------
//  Window
//--------------------------------------------------------------------------

// MarshalBinary serializes the internal properties of this Window instance
// into a byte array
func (target *Window) MarshalBinary() (data []byte, err error) {
	ctx := &EncodingContext{
		Buffer: util.NewBuffer(),
		Table:  nil,
	}

	e := target.MarshalBinaryWithContext(ctx)
	if e != nil {
		return nil, e
	}

	encBytes := ctx.Buffer.Bytes()
	return encBytes, nil
}

// MarshalBinaryWithContext serializes the internal properties of this Window instance
// into a byte array leveraging a predefined context.
func (target *Window) MarshalBinaryWithContext(ctx *EncodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	buff.WriteUInt8(KubemodelCodecVersion) // version

	// --- [begin][write][alias](Resolution) ---
	if ctx.IsStringTable() {
		a := ctx.Table.AddOrGet(string(target.Resolution))
		buff.WriteInt(a) // write table index
	} else {
		buff.WriteString(string(target.Resolution)) // write string
	}
	// --- [end][write][alias](Resolution) ---

	// --- [begin][write][reference](time.Time) ---
	b, errA := target.Start.MarshalBinary()
	if errA != nil {
		return errA
	}
	buff.WriteInt(len(b))
	buff.WriteBytes(b)
	// --- [end][write][reference](time.Time) ---

	return nil
}

// UnmarshalBinary uses the data passed byte array to set all the internal properties of
// the Window type
func (target *Window) UnmarshalBinary(data []byte) error {
	var table []string
	buff := util.NewBufferFromBytes(data)

	// string table header validation
	if isBinaryTag(data, BinaryTagStringTable) {
		buff.ReadBytes(len(BinaryTagStringTable)) // strip tag length
		tl := buff.ReadInt()                      // table length
		if tl > 0 {
			table = make([]string, tl, tl)
			for i := 0; i < tl; i++ {
				table[i] = buff.ReadString()
			}
		}
	}

	ctx := &DecodingContext{
		Buffer: buff,
		Table:  table,
	}

	err := target.UnmarshalBinaryWithContext(ctx)
	if err != nil {
		return err
	}

	return nil
}

// UnmarshalBinaryWithContext uses the context containing a string table and binary buffer to set all the internal properties of
// the Window type
func (target *Window) UnmarshalBinaryWithContext(ctx *DecodingContext) (err error) {
	// panics are recovered and propagated as errors
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(error); ok {
				err = e
			} else if s, ok := r.(string); ok {
				err = fmt.Errorf("Unexpected panic: %s", s)
			} else {
				err = fmt.Errorf("Unexpected panic: %+v", r)
			}
		}
	}()

	buff := ctx.Buffer
	version := buff.ReadUInt8()

	if version > KubemodelCodecVersion {
		return fmt.Errorf("Invalid Version Unmarshaling Window. Expected %d or less, got %d", KubemodelCodecVersion, version)
	}

	// --- [begin][read][alias](Resolution) ---
	var a string
	var c string
	if ctx.IsStringTable() {
		d := buff.ReadInt() // read string index
		c = ctx.Table[d]
	} else {
		c = buff.ReadString() // read string
	}
	b := c
	a = b

	target.Resolution = Resolution(a)
	// --- [end][read][alias](Resolution) ---

	// --- [begin][read][reference](time.Time) ---
	e := &time.Time{}
	f := buff.ReadInt()    // byte array length
	g := buff.ReadBytes(f) // byte array
	errA := e.UnmarshalBinary(g)
	if errA != nil {
		return errA
	}
	target.Start = *e
	// --- [end][read][reference](time.Time) ---

	return nil
}
